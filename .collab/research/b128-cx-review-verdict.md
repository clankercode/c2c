VERDICT: FAIL

major — .collab/research/2026-07-11T11-59-43Z-b128-generalize-arg-passthrough-audit.md:20-31; ocaml/cli/c2c_codex_cmd.ml:187-190; ocaml/c2c_codex_session.ml:300-345,376-391; ocaml/c2c_codex_app_server.ml:824-827 — The audit's central path claim is false for the supported Codex app-server mode. `start_delegate` calls `C2c_codex_session.run`; with `--app-server` or `C2C_CODEX_APP_SERVER=1`, it takes `run_app_server`, which builds `extra_frontend_args` and appends them in `build_frontend_argv`, without reaching `C2c_start.cmd_start` or `prepare_launch_args`. The direct app-server append preserves the tail, so this is not evidence of a runtime forwarding failure, but it disproves the claimed one shared `args @ extra_args` funnel and leaves that route untested. Proposed fix: correct the audit to describe the separate app-server path and add a direct app-server frontend-argv suffix test (or refactor both paths to one shared tail-assembly helper).

major — ocaml/test/test_c2c_start.ml:292-310 — The new Kimi and Gemini tests do not prove verbatim tail passthrough. `has_adjacent_pair` only proves that one selected pair occurs consecutively somewhere in the resulting argv; it permits insertion before/between other arguments, duplication, or a non-tail placement. The Kimi case is hermetic: its explicit `--mcp-config-file` makes `KimiAdapter.build_start_args` skip the config-file write. Proposed fix: assert that the final `List.length extra_args` elements equal the complete input list for both clients, including comma-bearing tokens.

major — ocaml/test/test_c2c_start.ml:318-357 — The boundary regression term declares `-n`/`--name` and `--model`, but not the real `--alias` option it claims to cover. It therefore proves `--model` and `-n` are not parsed by the reduced term, while `--alias` is only an undeclared post-boundary token. Proposed fix: add the real `--alias` option to the test term, capture it, and assert it remains `None`; preferably exercise a shared extraction of the production Cmdliner term so the grammar cannot drift.

minor — docs/commands.md:920,932-965,1078; ocaml/cli/c2c_setup.ml:1923-1924 — The new primary row correctly includes Gemini and excludes Pi, but the duplicate Tier 2 `start` row still omits Gemini and the new `[-- client-options...]` grammar. `c2c start --help` also uses `start_client_list`, which likewise omits the valid Gemini client. In addition, the new "every wrapper ... client's argv" wording does not describe the documented tmux exception (its tail is typed into a target pane). Proposed fix: update both client lists and the Tier 2 signature, and scope the general rule to the five managed agent clients or call out tmux/pty's existing special handling.
