# Status for Max — storm-beacon (c2c-r2-b2)

**Time:** 2026-04-13T02:37:30Z
**Session:** `d16034fc-5526-414b-a88e-709d1a93e345`

## Ready for your review (uncommitted)

1. **`c2c_poker.py`** (new) + **`CLAUDE.md`** (one-line entry under Python Scripts)
   - Generic PTY heartbeat poker for Claude / OpenCode / Codex / any PID.
   - Resolves target via `--claude-session NAME`, `--pid N` (walks parents
     via existing `find_terminal_owner` in `claude_list_sessions.py`), or
     explicit `--terminal-pid P --pts N`.
   - Verified end-to-end against my own session: `--pid 1106932 --once`
     resolved `(terminal_pid=3725367, pts=30)`, injected envelope landed
     in the next transcript turn as a user message.
   - Per your explicit request from earlier.

2. **Broker liveness** — `ocaml/c2c_mcp.{ml,mli}` + `ocaml/test/test_c2c_mcp.ml`
   - `registration.pid : int option` (None = legacy / treat as alive).
   - `Broker.register ~pid`, `registration_is_alive` via `/proc/<pid>`.
   - `enqueue_message` picks the first live alias match; raises
     `Invalid_argument "recipient is not alive: <alias>"` when all matches
     are dead (distinct error from unknown alias).
   - `handle_tool_call "register"` now captures `Unix.getppid()`.
   - 4 new tests: dead recipient, zombie-with-live-twin, legacy pid-less
     entry still delivers, pid round-trips through JSON. **18/18 pass.**
   - Full writeup: `.collab/findings/2026-04-13T02-35-00Z-b2-broker-liveness.md`

## Nothing committed

Per global instructions, I don't commit without explicit ask. When you're
back, two logical commits:
- `c2c_poker.py` + `CLAUDE.md` — generic PTY heartbeat tool
- broker liveness + tests — one OCaml commit

## Known-open follow-ups (flagged, not done)

- `Broker.register` still does `load → filter → save` without a file lock.
  The 01:55Z registry purge race is still there. Python `c2c_registry.py`
  has the reference `fcntl.flock` pattern — I'd mirror it with
  `Unix.lockf` on a `.lock` sidecar. ~30 lines. Can pick this up next tick
  if you want it without waiting for commit approval — purely additive.
- No `sweep` tool to drop zombie registrations. `registration_is_alive` is
  already exposed as a building block; a new MCP tool would be ~20 lines.
- `Unix.getppid()` at register time is captured once; a reparented MCP
  server (client dying) reports ppid=1 which `/proc/1` reports alive.
  Fix: re-capture at first RPC. Flagged in storm-echo's receiver-analysis §6.

## Loop cadence

- CronCreate `d08f4d6c`: `*/10 * * * *` (every 10 min), re-enters `/loop 10m`.
  (First time using CronCreate in this session after you showed the Apr 13
  11:15am scheduled-task fire from a far-off session — my earlier
  "cron is broken" conclusion was wrong.)
- Earlier ScheduleWakeup fallbacks (12:39, 12:47) are still pending.
  These overlap with the cron by a few minutes — expect one extra tick
  before the 10m cadence settles.

## What I'm doing between ticks

Holding on the uncommitted work. Happy to pick up registry locking next
unless you point me somewhere else.
