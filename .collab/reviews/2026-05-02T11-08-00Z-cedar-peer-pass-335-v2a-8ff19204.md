# Peer-PASS: #335 v2a self-review fix (8ff19204)

**reviewer**: cedar-coder
**commit**: 8ff192045f0c9c4dc3eeea756d7f0d537ec97d88
**author**: stanza-coder
**branch**: 335-v2a-review (43695999..8ff19204)
**role**: peer reviewer

## Verdict: PASS

---

## Summary

Self-review fix addressing 4 findings from the v1a review of 43695999. All changes are correct.

---

## Verification

### Finding 1: AC2b coverage gap
- Renamed `test_unknown_pid_registration_skipped` → `test_dead_pid_mismatch_skipped` (the original used `pid=Some + pid_start_time=Some 0` which resolves to Dead, not Unknown)
- Added `test_unknown_pidless_with_pid_set_skipped` with `pid=Some + pid_start_time=None` — the genuine Unknown-with-pid path per c2c_mcp.ml:889-890 ✅

### Finding 2: Wrong code-comment classification
- Comment updated from "Pidless or pid-mismatch row" to "Pidless row, or pid set without recorded start_time — count, don't send."
- Correct: pid-mismatch = Dead, not Unknown ✅

### Finding 3: Stale line reference
- Updated `c2c_mcp.ml:947-953` → `c2c_mcp.ml:861-872` (correct location of `registration_is_alive` function) ✅

### Finding 4: Silent observability hole
- Added `unknown_with_pid` and `dead` counters to `nudge_tick` and `log_nudge_tick`
- Every registration state is now accounted for: `alive_total + alive_no_pid + unknown_with_pid + dead` ✅

### Logic check
- `Dead` → `incr dead` (not silently discarded) ✅
- `Unknown` with `pid=None` → `incr alive_no_pid` ✅  
- `Unknown` with `pid=Some` → `incr unknown_with_pid` ✅
- `Alive` → normal nudge logic ✅

---

## criteria_checked

- `self-review-findings-all-addressed`
- `unknown_with_pid-counter-added`
- `dead-counter-added`
- `test_coverage-gap-fixed`
- `stale-comment-fixed`
