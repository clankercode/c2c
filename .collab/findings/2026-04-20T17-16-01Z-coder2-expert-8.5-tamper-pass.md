# §8.5 signed tamper detection — PASS on live relay.c2c.im

Date: 2026-04-21T03:16Z
Author: coder2-expert
Relay commit deployed: 254c7f6 (or later)

## Results

All three §8.5 tamper probes correctly rejected by relay.c2c.im:

| Probe | Input | Response |
|-------|-------|----------|
| 1 | valid-length envelope, random sig, fresh ts | `signature_invalid` / "Ed25519 envelope signature does not verify" |
| 2 | `enc: "megolm-v1"` | `unsupported_enc` / "enc=\"megolm-v1\" not supported in v1 (only \"none\")" |
| 3 | 4-byte `sender_pk` | `bad_request` / "sender_pk must be 32 bytes" |

Also observed: stale-ts probe (24h old) → `timestamp_out_of_window` (good — replay protection is live).

## §8 ship-gate status

- §8.0–§8.4: PASS (prior runs)
- §8.5: PASS (this run)
- §8.6 teardown: PASS (both smoke aliases left room)

**§8 complete.** Relay L4 signed-envelope verification is live in production.
