#!/usr/bin/env bash
# Deprecated user-facing wrapper (B122). Python setup path is retired.
set -euo pipefail
cat >&2 <<'EOF'
error: c2c-setup is retired (Python MCP path).

Use the OCaml installer instead:
  c2c install <claude|codex|opencode|kimi|grok>

MCP server binary: c2c-mcp-server (not python3 c2c_mcp.py)
EOF
exit 1
