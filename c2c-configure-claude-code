#!/usr/bin/env bash
# Deprecated user-facing wrapper (B122). Python configure paths are retired.
set -euo pipefail
cat >&2 <<'EOF'
error: c2c-configure-claude-code is retired (Python MCP path).

Use the OCaml installer instead:
  c2c install claude

MCP server binary: c2c-mcp-server (not python3 c2c_mcp.py)
EOF
exit 1
