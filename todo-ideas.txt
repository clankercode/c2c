# Ideas (not yet ingested)

This file is for ideas we haven't processed yet. Mostly it will be Max posting stuff here for processing later. Ideas should be handled intelligently, and ideas/input sought from agents. Make it a communal process.

It should be committed to git regularly so it's obvious when there's new stuff to ingest.

## Statuses

- `new` — just arrived, not yet discussed
- `brainstorming and planning` — swarm is exploring shape; comments being gathered
- `ingested` — moved into active scope (added to `todo.txt` / `todo-ongoing.txt` / a design doc); can be deleted from here

## Handling process

1. **coordinator1 sweeps this file** at each sitrep — promotes `new` items to `brainstorming and planning` by starting a short discussion (DM/room) with relevant peers.
2. **Authors add context**: each idea should gain a few bullets (use-case, rough scope, blockers) as agents discuss.
3. **Promote to ingested**: when the idea has a concrete home (a todo entry, a design doc, a project in `todo-ongoing.txt`), mark `ingested` and remove from this file (preserved in git history).
4. **Kill ideas that don't fit**: if the swarm decides an idea is out of scope, mark `ingested` with a note explaining why (so it doesn't re-surface).

## Entry template

```
---

Idea: <one-line summary>

Submitted by: <alias> · <UTC timestamp>
Status: new | brainstorming and planning | ingested
Related: <links to todos, findings, design docs as discussion accretes>

<context, details, open questions>
```

---

Idea: Use c2c relay to communicate with a remote node capable of doing GUI work.

Status: **ingested**

Minimal path moved to todo-ongoing.txt as "Remote relay transport" project.
Long-term E2E encrypted relay tracked in todo4.txt (separate project).

---

Idea: c2c git proxy extensions — signing + attribution

Submitted by: Max Kaye (via coordinator1) · 2026-04-24T17:05Z
Status: **ingested**
Related: #119 (c2c git proxy shipped), #129 (signing slice 1), design doc `.collab/design/2026-04-24-per-agent-git-signing.md`

Evolved through swarm discussion (Max + test-agent 2026-04-24):
don't detect user's global signingkey at all; c2c manages its own
per-repo, per-agent keys (reuse the Ed25519 identity that
`c2c register` already generates). SSH-format git signatures.
Attribution via `--author="<alias> <alias@c2c.im>"`. Full design
captured in the design doc; sliced into #129+ for implementation.

---

Idea: PoW-gated email proxy for agent commit attribution at c2c.im

Submitted by: Max Kaye (via coordinator1) · 2026-04-24T17:02Z
Status: brainstorming and planning
Related: git attribution convention (CLAUDE.md), todo.txt #119, swarm-lounge discussion 2026-04-24T11:53 AEST

Context:
Agent commits should use `--author="<alias> <alias@c2c.im>"` so:
1. attribution is clear in git log (who on the swarm did what)
2. the email is real advertising for c2c.im

To make the email genuinely contactable without enabling spam, run a PoW-gated
email proxy: sender must submit a proof-of-work to deliver a message, so bulk
spamming is economically unattractive but a single interested human can always
reach through.

Immediate slice (SHIPPED 2026-04-2x — git.attribution config + `--author` injection):
- ✅ `git.attribution` config schema landed (`ocaml/c2c_start.ml:498` `repo_config_git_attribution`)
- ✅ `c2c git` shim wires `--author="<alias> <alias@c2c.im>"` (`ocaml/cli/c2c.ml:2081`)
- ✅ Placeholder alias@c2c.im in use; bounces are fine pending PoW proxy
- ✅ CLAUDE.md convention documented

Future slice (PoW proxy) — design decisions locked 2026-04-28 by Max:
- Stand up email proxy at c2c.im that accepts mail only after valid PoW header
- Wire MX record; any alias@c2c.im routes through proxy
- **PoW scheme**: hashcash with **dynamic difficulty**. Each agent sets its own
  difficulty target (advertised via DKIM-style header or profile-page lookup).
  Senders compute hashcash to match the recipient agent's target.
- **Bounceback on insufficient PoW**: emails that fail to meet the target
  difficulty get a bounce reply with the required difficulty + sample
  hashcash invocation, so legitimate senders can retry.
- **Recipient routing**: per-alias inbox routing (each alias gets its own
  inbox at the proxy; cross-references with c2c profile page for the
  optional link-back).
- **Profile-page link**: yes — emails that pass PoW carry a header/footer
  pointing to the sender agent's public c2c profile (depends on c2c GUI
  profile page progress).

---

Idea:

---
