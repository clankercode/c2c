---
claimed_at: "2026-07-21T15:47:20Z"
claimed_by: cli-user
completed_at: "2026-07-21T17:34:07Z"
complexity: high
created_at: "2026-07-21T13:50:18Z"
depends_on:
    - B265
duration_minutes: 106.7866909629
estimate_hours: 8
id: B266
priority: critical
started_at: "2026-07-21T15:47:20Z"
status: done
tags: []
title: Migrate relay installations to private fail-closed reachability
updated_at: "2026-07-21T17:34:07Z"
---
## Purpose

Ship a safe operator/user migration from current global authenticated-peer discovery and default-allow inbound policy to private, consent-gated reachability.

## Work

- Define upgrade behaviour for existing registrations, outboxes, inboxes, policies, address books, pins, managed sessions, and remote aliases.
- Default production to fail closed after the approved compatibility boundary; never silently preserve unsolicited first-contact reachability.
- Provide explicit commands/configuration to issue/share/revoke/rotate contact routes and to audit current exposure without printing reusable secrets accidentally.
- Make development bypasses explicit, noisy, and excluded from production security claims.
- Update doctor/health diagnostics to detect legacy default-allow, missing migration, insecure endpoint schemes, stale grants, and mixed-version downgrade risk.
- Document operational recovery and rollback without weakening the invariant.

## Acceptance evidence

- Upgrade tests cover old state, mixed versions, interrupted migration, retry, rollback, and idempotence.
- CLI/help/doctor output is tested and does not leak capabilities.
- A production deployment cannot report healthy while retaining unintended global discovery/default-allow admission.

## Post-B219 migration constraints (from B261)

- Available pattern today: `CREATE TABLE IF NOT EXISTS` in `sqlite_ddl` + create-time column probes/ALTER. `schema_version` is declared but unused as a general driver.
- Existing best-effort `Sqlite3.exec ... |> ignore` is **not** acceptable for security-critical migration; need durable migration/feature state and checked failures.
- Rolling back to binaries that ignore grant/private flags reopens global reachability — need a compatibility floor or refuse-old-binary marker, not only a DB backup.
- Tokenless development mode cannot substantiate private-reachability claims.
- Design input: `.collab/research/2026-07-22-b261-private-reachability-persistence-reconciliation.md`.

## Independent review evidence gaps

Add dedicated doctor aggregate/exit-status coverage for insecure production states, and CLI lifecycle output tests proving list/revoke never expose reusable secrets. `contact issue --json` intentionally outputs the one-time secret; document that stdout must be protected from logs/history and test that other commands do not repeat it.