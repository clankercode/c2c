---
complexity: medium
depends_on: []
estimate_hours: 1
id: B085
priority: high
status: pending
tags: []
title: 'per-thread codex hook auto-registrations accumulate forever (every codex exec conversation leaves an unknown-liveness registry row). Need GC: deregister on codex SessionEnd hook if available, else TTL sweep for unknown-liveness codex-* rows idle >N hours (careful: sweep is dangerous for live sessions, prefer SessionEnd + lazy expiry on read)'
---

per-thread codex hook auto-registrations accumulate forever (every codex exec conversation leaves an unknown-liveness registry row). Need GC: deregister on codex SessionEnd hook if available, else TTL sweep for unknown-liveness codex-* rows idle >N hours (careful: sweep is dangerous for live sessions, prefer SessionEnd + lazy expiry on read)
