# c2c TODO list

This file is the rolling bug/feature backlog for the c2c swarm. Anyone (human
or agent) may append items; use `- [ ] FOLLOW-UP (v2): per-instance shim → swarm-wide shim. Currently each agent installs the #452 pre-reset-hook shim manually to their instance bin dir. v2: shim lives at `~/.local/bin/c2c-git-shim`, and `c2c start <client>` adds it to the spawned shell's PATH automatically. Closes the per-instance surface so all peers are protected. Lead: jungle-coder. Filed 2026-04-29.
  Finding: `.collab/findings/2026-04-29T04-30-00Z-jungle-coder-452-test-methodology-leak.md`
  Shim: `.worktrees/pre-reset-hook/git-shim.sh` (committed `f14c5f34` + `e297315a`)

` for open, `- [x]` for done. Keep
entries short — link to `.collab/findings/*.md` for details. Include your alias
and date on non-trivial entries so future readers have a contact point.

**Hygiene:** periodically prune completed (`- [x]`) items that are more than
a week old and whose context is no longer load-bearing. Keep recent closed
items for a few days so the team can see what just shipped; archive the rest
by deleting — git history keeps them if anyone needs to look back.

---

- [x] opencode plugin permission reply: galaxy-coder's instance registers as "current-session" but c2c plugin permission requests route to "galaxy-coder" (the session_id). Routing works correctly — broker maps session_id→alias. Confirmed by galaxy-coder in testing (2026-04-22).
  `c2c start codex-headless` now probes `--thread-id-fd`, persists opaque bridge thread ids lazily, and reuses them on resume.
  Managed headless delivery now stages broker messages through the durable XML spool and writes them through the named-pipe bridge path.
  Headless launches now reserve bridge-owned stdin via named FIFO delivery; no PTY fallback path remains on the managed bridge transport.
  Capability-gated live tmux coverage now exercises startup plus first-message thread-id persistence on the landed Codex binaries.
- [x] FOLLOW-UP (after current terminal E2E work): the new Codex binaries have landed. Revisit the blocked Codex / codex-headless plan items that were gated on missing binary support, then re-plan and implement the newly unblocked slices (especially updated TUI XML / bridge capability paths and their live tests). (implemented by f2ddb7b and verified live on 2026-04-22)
- [x] EPIC: OpenCode agent-file system. Leads: coordinator1 (design doc), jungel-coder (OCaml implementation), galaxy-coder (renderer templates), coordinator1 (docs + migration). Shape evolved: c2c as hub compile model. Canonical: `.c2c/roles/<name>.md` (YAML frontmatter + body). Compile on-the-fly to `.opencode/agents/<name>.md`, `.claude/agents/<name>.md`, etc. Git policy: compiled artifacts are NOT checked in (gitignored). Implementation: pure OCaml. Design doc: `.collab/agent-files/design-doc-v1.md` (v7, schema locked). **v1.1 DONE** — all blockers closed including subgroup refactor (a60ed9e). Codex renderer shipped in 35da2d4, Kimi renderer in 894cde3 — `c2c roles compile --client all` writes to opencode/claude/codex/kimi agent dirs. Residual: optional Codex/Kimi native agent-file-spec compatibility (post-v1.1). Missing-role authoring done: dogfood-hunter, release-manager, gui-tester committed 78d91cc + 6829385.
- [x] PERF/UX: `send_room` broadcast delivery is slow to idle opencode peers (~30s per observation). Root cause: plugin's `C2C_MCP_INBOX_WATCHER_DELAY` defaults to 30s to let preferred paths (PostToolUse, PTY sentinel) win first. Idle agents have no preferred path, so they wait the full delay. Active agents get faster delivery via tool-call hooks. Fix: reduce default from 30s to 5s (commit 6ccc205). 5s is short enough to keep idle agents responsive while still giving active agents' preferred paths time to win the race. Reported by Max 2026-04-22 09:39 UTC. Blocks production-feel for relay.
- [x] SECURITY (permission/question flow): reply-to alias-spoofing. M3 (expectedSenderAlias binding in pendingPermissions + delivery verification) committed by ceo in 15713e9. M4 (alias reuse refused while prior owner has pending state) now implemented in commit e86101b — guard no longer requires alive prior registration; blocks on orphaned pending permissions alone. Reported by Max 2026-04-22 19:32 local.
- [x] URGENT BUG (agent-file regression introduced by e57f0c1): empty-scalar fix broke namespace block emission. Fixed in e11afeb (namespace rendering) + 6fd3ec4 (claude prefix strip). Coordinator1 verified 2026-04-22 19:40 UTC.
  - registration schema: add `compacting: { started_at: <epoch>, reason?: <str> } | null` on the per-alias registration row.
  - setter path: client-side hook (Claude PreCompact already wired for cache-hint emission; extend to flip the flag + clear on PostCompact). OpenCode: /compact wrapper. Kimi/Codex: TBD.
  - send-side warning: `c2c_send` checks recipient.compacting on dispatch; returns `{queued: true, warning: "recipient compacting for Ns"}` — still queues, don't drop (compaction ends).
  - stale-flag guard: auto-clear after N minutes (compaction shouldn't exceed ~5min); GC sweep picks up stragglers.
  - Ties into "agent self-compaction" item above — once that lands, flag-flip becomes authoritative.
  Commits: f0f31ed (schema + send-side warning), f22ae8a (persistence + CLI warning), d139490 (session.compacted → clear-compact), a90a7ed (PLUGIN_VERSION wiring)
- [x] review all c2c commands for safety tier classification. Tier1: safe for agents (messaging, queries). Tier2: safe with care (lifecycle). Tier3: unsafe for agents (systemic). Tier4: internal plumbing. Implemented: `commands_man()` for tier-grouped help, `filter_commands()` to hide Tier3+4 when C2C_MCP_SESSION_ID is set, `command_tier_map()` for per-command classification. Compile-time GADT enforcement deferred to v2. jungel-coder 2026-04-22.
- [x] (screenshot updated 7f1465d) BUG (c2c envelope): the `<c2c event="message" ...>` envelope needs an explicit field for reply medium (almost always `c2c_send`, but could be room, email, webhook, out-of-band, etc. in future). Current envelope has `from`/`alias`/`source`/`action_after` but no `reply_via` — recipients infer they should use `c2c_send` from convention, which is fragile and makes non-c2c-sourced messages (e.g. relay-bridged, mobile-app) ambiguous. Proposed shape: `reply_via="c2c_send" to="<alias>"` attribute. Screenshot example: `tmp/screenshots/c2c-reply-text.png`. Filed by Max 2026-04-22. (Originally committed as 0335e64, orphaned by a rebase — re-added here.)
- [x] (dogfooded + PostCompact hook added) FEATURE (statefile: Claude Code): complete compacting-status integration for Claude Code. Path: plugin-handled. Claude PreCompact hook at `~/.claude/hooks/c2c-precompact.sh` (shipped this hour by galaxy-coder) calls `c2c set-compact`; PostCompact equivalent calls `c2c clear-compact`. Dogfood verification: end-to-end live test currently in progress by galaxy-coder as of 2026-04-22 21:13 local. Close this once the dogfood finding is filed green. Filed 2026-04-22 by Max.
- [x] FEATURE (statefile: Codex headless): NOT FEASIBLE — Codex headless has no in-process compaction mechanism (no PreCompact/PostCompact hooks, no session.compacted event, no thread_id recycling signal). `codex-turn-start-bridge` exposes no compaction-related flags or hooks. Unlike Claude Code (item 78) and OpenCode (item 93), there is no lifecycle event to hook into. Kimi + Crush deferred until we have a first-class story. Filed 2026-04-23 by Lyra-Quill. Research: jungle-coder + ceo confirmation.
- [x] BUG (CLI): `-h` does not produce help on any `c2c` subcommand — only `--help` works. Should be an alias for `--help` on every command (root + all subgroups + all leaves). Standard Unix convention. Fix likely in the cmdliner setup to add `-h` as an alias for the help flag across the tree. Filed by Max 2026-04-22.
- [x] BUG (wizard UX): `c2c agent new` without NAME errors out instead of prompting. If the TTY is interactive, should prompt "Role name: " with validation (no dupes, valid filename chars) then proceed with the rest of the interactive wizard. Filed by Max 2026-04-22.
- [x] BUG (delivery): DMs sent post-swarm-kill were queued but never delivered. Root cause chain: (1) stray `}` in c2c.ts 51cba21 broke plugin load on all managed peers (fixed 9d2274a); (2) broker background watcher silently drained Claude Code inboxes (fixed 6946b07 — watcher now checks channel_capable_ref). End-to-end live-verified 2026-04-22 with roundtrip DMs to coordinator1, galaxy-coder, jungel-coder + on-demand permission relay. coordinator1 2026-04-22.
- [x] BUG (lifecycle): on peer restart, the "you are a c2c agent" auto-message no longer fires. FIXED: deliverKickoffPrompt now called from applyRootSessionCreated (after coldBootDelayMs), so warm restarts get kickoff regardless of session.idle. Commit 1f3120c. Filed by Max 2026-04-22. Fixed by jungle-coder 2026-04-23.
- [x] BUG (statefile freshness) — partial fix: is_idle now set false on session.created. Remaining: session.start/step handlers (may already be covered). coordinator1 2026-04-22.

- [x] our agent files / templates should carry metadata or a section that contians recovery instrucitons after being restarted. this should include, for example, the monitors that should be reestablished (4 minutes is for everyone always). Fixed: all 10 builtin roles now include: [recovery] pointing to .c2c/snippets/recovery.md with standard post-restart instructions. jungel-coder 2026-04-22.
- [x] BUG (restart-self kills managed OC): galaxy-coder ran `./restart-self` after `just install-all` to pick up plugin fix 9d2274a — whole managed process tree died, Max had to manually relaunch. `restart-self` apparently kills the `c2c start` supervisor instead of cycling just `.opencode`. Fix: (a) restart-self now detects c2c start supervisors via cmdline inspection and refuses with safe-recipe message; (b) .opencode/plugins/CLAUDE.md documents safe restart. See .collab/findings/2026-04-22T12-58-00Z-coordinator1-restart-self-kills-managed-oc.md. jungel-coder 2026-04-22.
- [x] BUG (plugin silent break): single stray `}` in .opencode/plugins/c2c.ts (commit 51cba21) silently broke plugin load for every managed OpenCode session started after — no DMs, no statefile, no permission relay. Fix: (a) smoke test `c2c-plugin.smoke.test.ts` catches this in 50ms via `just test-ts`; (b) pre-commit hook `scripts/git-hooks/pre-commit` runs `bun build .opencode/plugins/c2c.ts` on staged plugin changes. Installed via `just install-git-hooks`. coordinator1 2026-04-22.
- [x] mail delivery issues starting at 10pm TPK courtesy of a pkill on c2c binaries. Root cause: galaxy-coder ran `pkill -f "c2c "` to work around a busy-binary install error, killing the entire swarm. Recovery: cherry-picked orphaned commits; told galaxy-coder to write up the incident. Guardrail: CLAUDE.md updated + auto-memory entry — never pkill on `c2c` as a match pattern; `just install-all`'s atomic rm+cp handles busy binaries. No tooling fix beyond the doc. coordinator1 2026-04-22.
- [x] BUG (broker watcher drains Claude Code inboxes silently): broker MCP background watcher drains messages after a 5s delay and emits notifications/claude/channel; Claude Code doesn't surface that notification method, so if no Bash tool call happens in that window, messages land in archive and are LOST from the agent's perspective. Repro: DM a coordinator1 Claude session while it's waiting on user input → message appears only in archive/coordinator1.jsonl. Confirmed 2026-04-22 with galaxy-coder's permission DM. Fix: commit 6946b07 — watcher now checks channel_capable_ref before draining; non-channel clients leave messages in inbox. See .collab/findings/2026-04-22T13-01-00Z-coordinator1-watcher-drains-messages-silently.md. jungel-coder 2026-04-22.
- [x] BUG (tiers regression): FIXED in 3c15da5 (monitor Tier1, unknown-default Tier2) + f582236 (screen Tier1). Verified by galaxy-coder 2026-04-22. `c2c monitor` is UNRUNNABLE when C2C_MCP_SESSION_ID is set. 610aa48's tier filter removes tier-3 commands from the cmdliner command list entirely, not just from --help. But CLAUDE.md tells every agent to arm `c2c monitor --archive` on startup and the recovery snippet (include: [recovery]) in all 10 builtin roles relies on it. Result: no agent can arm the inbox-watcher Monitor that would wake them on DMs. Fix: either (a) move `monitor` (and any other read-only observation commands like `screen`, `tail-log`) to Tier 1/2, OR (b) change tier filter to hide from --help but still accept invocation. (a) preferred — monitor is read-only. Workaround: raw `inotifywait -m .git/c2c/mcp/ | grep archive/`. coordinator1 2026-04-22.
- [x] BUG (idle-session delivery gap): FIXED end-to-end — 6946b07 (watcher gate) + 3c15da5/f582236 (monitor tier). Peers re-arm `c2c monitor --archive`; inbox writes wake immediately. after 6946b07, messages correctly remain in Claude Code inboxes (good — no more silent archive drain). But idle Claude Code sessions never poll unless a Bash tool call fires the PostToolUse hook. Observed 2026-04-22 when galaxy-coder was stuck waiting on a permission approval: DMs queued to coordinator1+ceo inboxes at 13:32, neither polled until heartbeat cue at 13:33:30. 90s delay effectively idle-bombed galaxy. Mitigation: every Claude Code agent should arm `c2c monitor --archive` (inotify-based Monitor) so inbox changes wake the model immediately — CURRENTLY BLOCKED by the tiers regression above. Until that's fixed, use raw inotifywait as Monitor command. Permanent fix: wake-on-file-event via the Monitor tool is the right shape; tighten heartbeat isn't. coordinator1 2026-04-22.
- [x] BUG (permission request not delivered real-time): FIXED — resolved by idle-session delivery gap fix (see above). Max observed galaxy-coder stuck behind permissions; neither coordinator1 nor ceo received the request in real time (surfaced ~90s later on heartbeat poll). Root cause is the idle-session delivery gap above. Fix resolves both. coordinator1 2026-04-22.
- [x] FEATURE (delivery): NOT FEASIBLE — Claude Code has no TypeScript plugin SDK; only shell hooks (PostToolUse, PreCompact, etc.) which cannot run background tasks. Monitor + hooks is the chosen path. Research finding: .collab/findings/2026-04-23T13-55-00Z-jungel-coder-item-115-claude-code-plugin-research.md. jungel-coder 2026-04-23.
- [x] ingest data/c2c_alias_words_toingest.txt and filter, probably add ot the main wordlist next to it. ensure no dupes in final list. 1429 words merged; 1560 total. jungel-coder 2026-04-23.
- [x] CHORE (registry hygiene): sweep stale typo `jungel-coder` registration + duplicate `jungle-coder` pid=424242 entry. Cleanup done 2026-04-23 by galaxy-coder (typo entry removed, stale PID was already refreshed). Remaining open: should sweep auto-detect "alias exists live in instances/<name>/ but registry shows dead PID" and refresh? coordinator1 2026-04-22.
- [x] UX (c2c start): when `-n <name>` matches `.c2c/roles/<name>.md` and `--agent` not passed, auto-apply `--agent=<name>`. Current: `c2c start codex --agent Lyra-Quill -n Lyra-Quill` is needlessly verbose. Proposed: `c2c start codex -n Lyra-Quill` infers the role. Explicit `--agent` still overrides. Filed by Max 2026-04-22. coordinator1. Fixed in efbef72.

- [x] BUG (statefile token counts): `c2c statefile` reports `tokens_input: 189` and `tokens_output: 58` for Lyra-Quill, but the agent has 288 steps and 16 turns. These numbers are clearly wrong — likely counting only the current turn rather than accumulating across the session. Fixed by accumulating tokens_input, tokens_output, and tokens_cache_read across turns (12cba8c). Filed 2026-04-23 by Lyra-Quill.

- [x] FEATURE: `c2c start codex --agent <name>` — basic Codex renderer shipped in 35da2d4. `c2c roles compile --client codex` writes `.codex/agents/<name>.md` (mirrors claude_renderer shape). `c2c roles compile --client all` compiles every supported client in one pass. **Residual (separate follow-up)**: align output with Codex's native `AGENTS.md` at project root spec if/when needed — frontmatter schema for that is still research-gated (Codex docs don't formalize it). Filed 2026-04-23, basic closed 2026-04-23.
- [x] FEATURE: `c2c start claude --agent <name>` — verified working. Claude_renderer exists, --agent flag parses correctly. Session refused only because running inside c2c session (not a code bug). Filed 2026-04-23.
  - `c2c start claude --agent <name>` end-to-end (role compile → launch → first message round-trip)
  - `c2c start opencode --agent <name>` end-to-end
  - `c2c start kimi --agent <name>` end-to-end (requires live Kimi binary)
  - `c2c roles compile --client all` verified by spawning each client and checking the agent file is picked up
  Shared harness code should live under `tests/e2e/framework/` alongside the existing codex tests. Capability-gated (opt-in via env var; no-op in CI without binaries). Filed 2026-04-23 by Max. All pieces already implemented: framework in tests/e2e/framework/ (all 4 adapters + scenario fixture + conftest), e2e tests in tests/ (codex-headless smoke, opencode smoke+with_agent, kimi smoke+with_agent, claude smoke BLOCKED by workspace trust prompts). Roles compile --client all tested in test_c2c_roles_compile_e2e.py. Closed 2026-04-23 by galaxy-coder.

- [x] FEATURE (c2c roles compile): inject a default `steps: 9999` in OpenCode agent file frontmatter when the role doesn't specify `opencode.steps`. Context: OpenCode's per-agent `steps` field caps agentic iterations; default (~100) is often too low for long-running swarm agents. Top-level `steps` in `opencode.json` is ignored — must be per-agent. The c2c renderer already passes through `opencode.steps: X` if the role file sets it, but new roles shouldn't have to think about this. Scope: in `OpenCode_renderer.render` in `ocaml/c2c_role.ml`, if `opencode` entries don't contain a `steps` key, emit `steps: 9999`. Add a corresponding test in `ocaml/test/test_c2c_role.ml` asserting default gets injected AND user-provided opencode.steps takes precedence. Similar audit might be needed for codex/kimi/claude — check if any of those clients have equivalent per-agent iteration caps and apply same default-injection pattern. Filed 2026-04-23 by Max. Upstream reference: https://opencode.ai/docs/agents/ and ~/.llm-general/ai-coding/opencode/agents.md line 78. Committed 3b0d5f8 (ceo saved galaxy-coder's slice). Verified dry-run at 02:15 UTC. No codex/kimi/claude equivalent caps found — no injection needed for them. Closed 2026-04-23 by galaxy-coder.
- [x] add to CLAUDE.md or somewhere else that's suitable: use the review-and-fix skill after finishing work before returning it (commit your work before running review-and-fix and commit after, too). Also need to port .claude/skills/review-and-fix to codex format. Filed 2026-04-23 by Max. Closed 2026-04-23 by coordinator1-on-behalf: CLAUDE.md "Development Rules" gained a commit-before/after review-and-fix bullet pointing at both skill paths; skill ported to `.codex/skills/review-and-fix/SKILL.md` (same YAML frontmatter format as Codex system skills) and installed to `~/.codex/skills/review-and-fix/SKILL.md`.

- [x] UX (c2c agent new): accept optional positional `<role-name>` that prefills the `Role name:` prompt. Today `c2c agent new review-bot` errors with "too many arguments, don't know what to do with review-bot"; must run `c2c agent new` with no args and type the name at the interactive prompt. Not a bug (Max confirmed, 2026-04-23) — just rough UX. Filed 2026-04-23 by Max. Fixed 2ba78d7: error when both positional and --name given; 2 tests added.

- [x] BUG (c2c agent new clients parser): entering `123` for the "Compatible clients (comma-separated numbers)" prompt parses as `0` (all) instead of the intended `{1,2,3}` (claude+opencode+codex). Parser likely reads a contiguous digit run as a single-index lookup and silently falls through to the default when it doesn't match a defined client index. Should either (a) reject un-comma-separated digit runs with a clear error, or (b) interpret per-char if length>1 and every char is a digit. Filed 2026-04-23 by Max. Fixed: digit-splitting in `prompt_multi_select` (c2c.ml:6990-6991). Commits 5183def (rec fix) + prior HEAD digit-splitting logic.

- [x] DESIGN (ephemeral one-shot agents, from Max 2026-04-23): SHIPPED — spec promoted from DRAFT to SPEC at `.collab/design/SPEC-ephemeral-one-shot-agents.md` (commits 3805a697/b9aaa505). Primitive landed via #143 (commits 10310a7b spec, 42190629 mode dispatch + reply-to + no-autojoin, b129fd0d auto-join suppression, fe93c4a7 watchdog 60s grace).

- [x] IDEA (task-decomposition + planning agent, from Max 2026-04-23): SHIPPED — `planner1` role landed as #112 (commit c5a4e65b: task-decomposition agent that interviews peers and synthesizes slices).

- [x] IDEA (standby reviewer agents, from Max 2026-04-23): SHIPPED — `review-bot` role landed as #108 (commits 9b7a4f7c initial + ca6e19cf ephemeral standby reviewer with c2c_stop_self lifecycle, role_class: reviewer).

- [x] PROVISIONAL BUG (codex c2c start — 2 items, Max _ingest/bugs/codex-c2c-start.txt 2026-04-23): (1) `c2c_deliver_inbox.py` crashed on `import c2c_inject` after c2c_inject moved to deprecated/. **Addressed**: lazy-import at commit 4239326. (2) `c2c start codex` hangs on exit; ctrl+C → code 122. **Fixed**: WNOHANG waitpid in stop_sidecar (commit 497f9b2a, #258).

- [x] POST-M1 (message delivery latency, from Max 2026-04-23): design doc filed + 2 concrete wins landed. `.collab/design/DRAFT-delivery-latency.md` has full symptom survey. Win 1: watcher delay 5s→2s (commit 8171bc6). Win 2: poker interval 600s→180s (commit 8171bc6). Win 3 (channel_capability logging) deferred — needs separate review. Room fan-out async writes deferred to future optimization. Kimi wire bridge poll interval needs separate investigation.

- [x] FEATURE (pmodel preferences): `.c2c/config.toml` now has a `[pmodel]` table of per-role-class provider:model preferences, and role files may carry a `pmodel:` + `role_class:` frontmatter override. OCaml parser `C2c_start.parse_pmodel`, lookups `C2c_start.repo_config_pmodel_lookup`, and `C2c_role.resolve_pmodel` (role -> class -> default) landed with unit tests. **Wire-through to client launch**: done in commits 18f7f8ca + 15b6b92f + 7186f8de. Role pmodel advisory → 3-way priority: explicit --model > role pmodel > saved config. `role_class:` populated on all 13 builtin roles. Filed 2026-04-23 by coordinator1.
- [x] BUG (session resume): Claude started with custom profile / no OAuth is unable to automatically resume sessions (or auto-resume is not working). Needs investigation + fix. Filed 2026-04-24 by tundra-coder-live2. Fixed commit 6a68405: claude_session_exists now probes both ~/.claude/projects and $CLAUDE_CONFIG_DIR/projects. Path corrected in 74db661: uses $CLAUDE_CONFIG_DIR/projects (not .claude/projects). Peer-reviewed by galaxy-coder.

- [x] add to c2c xml, attribute for sender permissions or special role or sometihng. basically when a user is chatting to their agents, they should see the user has certain perms. absent for regular agents. maybe a `role` for special roles. SHIPPED as #107 — design 810deeb7, Slice 1 (registration role field) 91d0cdd2 + 0f00901c, Slice 2 (role attribute on c2c XML envelope + channel meta) c7256997.

- [x] IDEA (role-specific rooms, from Max 2026-04-24): when generating roles, agents should join not just swarm-lounge but also a role-specific room (e.g. all coders in #role-coders, all reviewers in #role-reviewers). Enables targeted broadcasts and team coordination. Natural companion to the standby reviewer agent concept (#108). File by coordinator1. **Implemented by jungle-coder — design doc: `.collab/design/2026-04-24-role-specific-rooms-implementation.md`**. Changes: `c2c_role.ml` adds `role_class_to_room` helper; `c2c_start.ml` `build_env` accepts `role_class_opt` and derives role room when `C2C_AUTO_JOIN_ROLE_ROOM=1`; `c2c.ml` `run_ephemeral_agent` derives role room from `role_class`; all 5 install setups add `C2C_AUTO_JOIN_ROLE_ROOM=1`; all builtin roles get `role_class:` field; unit tests added. Room naming: `role-<class>` (e.g. `role-reviewer`, `role-coder`). Dogfood pending.

