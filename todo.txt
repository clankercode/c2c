- [ ] `c2c start` should error if started from within a c2c session. It's okay for it to be spawned in tmux or whatever, but should not be run directly from an agent's bash tool. c2c should error with a message explainign this. 
- [ ] similar to joining, when an agent leaves/disconnects it should notify the room, too
- [ ] to autostart opencode wiht a prompt and have it execute, use `opencode --prompt '<the prompt>'`
- [ ] opencode still doesn't launch/resume right; use --prompt for new sessions, but need to use plugin injection for existing sessions. 
