# c2c TODO list

This file is the rolling bug/feature backlog for the c2c swarm. Anyone (human
or agent) may append items; use `- [x]` for open, `- [x]` for done. Keep
entries short — link to `.collab/findings/*.md` for details. Include your alias
and date on non-trivial entries so future readers have a contact point.

**Hygiene:** periodically prune completed (`- [x]`) items that are more than
a week old and whose context is no longer load-bearing. Keep recent closed
items for a few days so the team can see what just shipped; archive the rest
by deleting — git history keeps them if anyone needs to look back.

---

- [x] **#154**: `launch_args 81` extra_args reversed + at beginning not end. Root cause: `last_n` test helper had `(n<=0) -> List.rev acc` double-reversal; also `fast_path_get_tmux_location` broad `_` catch-all on orphaned TMUX_PANE. Fixed in `373f6a43` (cherry-picked `3ff1d866`) + `5ec45127` (cherry-picked `e1da2fad`). Severity LOW. Filed 2026-04-29 by jungle-coder.
  Finding: `.collab/findings/2026-04-29T13-30-00Z-jungle-coder-pre-existing-test-failures.md`

- [x] **#155**: `lookup 0` coder lookup returns wrong expected value. Root cause: `test_lookup_known` expected `role_template_src|---` but codegen correctly outputs YAML frontmatter `---`. Fixed test expectation in `5ec45127` (cherry-picked `e1da2fad`). Severity MEDIUM. Filed 2026-04-29 by jungle-coder.
  Finding: `.collab/findings/2026-04-29T13-30-00Z-jungle-coder-pre-existing-test-failures.md`

- [x] **#156**: `get_tmux_location 0` exits 0 when TMUX unset via env. Root cause: tmux shim passes through subcommand when TMUX_PANE is still set (even with TMUX unset). Fixed in `373f6a43` (cherry-picked `3ff1d866`). Severity LOW. Filed 2026-04-29 by jungle-coder.
  Finding: `.collab/findings/2026-04-29T13-30-00Z-jungle-coder-pre-existing-test-failures.md`

- [x] 🚨 BUG (CRITICAL — git shim self-exec recursion): SHIPPED 2026-05-02T01:22Z as 71227db5 (cherry-pick of stanza-coder's 85008c2b). `Git_helpers.is_c2c_shim` reads first 512B of each PATH candidate, refuses any containing the literal marker `# Delegation shim: git attribution for managed sessions.`; `find_real_git` now uses two-guard composition (`in_shim_dir || is_c2c_shim`); hard fallback to /usr/bin/git preserved. 8 unit tests (positive/negative/edge cases). Coord-PASS-under-emergency by coordinator1. Finding: `.collab/findings/2026-05-02T01-12-00Z-coordinator1-git-shim-self-exec-recursion-cpu-spike.md`. Two outages at 2026-05-01T23:00Z + 2026-05-02T01:00Z (load to 32) closed.

- [x] FEATURE (from Max 2026-05-02): `c2c start <client>` needs a CLI flag to skip any interactive prompts. SHIPPED as #614 (ac07b7f2) — `--no-prompt` / `--non-interactive` flag. willow-coder.

- [x] FEATURE (from Max 2026-05-02): in-transcript envelope HH:MM timestamp. SHIPPED as #615 (jungle verified in-transcript variant carries HH:MM via `format_c2c_envelope`). jungle-coder.

---

- [x] #625 — `c2c peer-pass send` hits git circuit breaker internally. Fixed: graceful degradation when breaker open (201d3d41) + reset breaker before validation to prevent self-sign anti-cheat bypass (7119c837). Peer-PASS stanza, coord-PASS coordinator1. Cherry-picked as 48f439d7 + a9c5f866. Closed 2026-05-02 by willow-coder.

- [x] #626 — Raise git circuit breaker default from 5 → 15 spawns/3s window. Fixed: cc867800 (willow), cherry-picked as 06145b8a. Closed 2026-05-02.
