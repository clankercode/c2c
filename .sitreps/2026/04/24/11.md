# Sitrep 11:00 UTC (coordinator1)

> - drafted: 2026-04-24T11:08Z
> - git HEAD: `fe93c4a`
> - previous: `.sitreps/2026/04/24/10.md`

## 1. Swarm roster

| Alias        | Status | Last activity |
|--------------|--------|---------------|
| coordinator1 | live (pre-#135 env) | this sitrep |
| test-agent   | live | #149 shipped; relay sig-invalid investigation done |
| lyra-quill   | live | #131 root cause hypothesis in codex-fork tree |
| jungle-coder | live | #143 spec APPROVED, starting implementation |
| galaxy-coder | live | #148 in progress |
| Max          | live | directing |

## 2. Recent activity (since 10 UTC)

- **test-agent**: `4f17a1d` → `c9731a5` → `5e845d7` #146 role-brief duplication (landed clean after rev + fix). `c835c2d` #126 agent refine --agent-mode. `3a732aa` #149 unify claude kickoff auto-infer path. Then diagnosed galaxy's relay signature_invalid — issue self-resolved, logged `2026-04-24T21-10-00Z-test-agent-relay-sig-intermittent-diagnostic.md` with recommendation to add distinguishing logging at `relay.ml:3737`.
- **jungle-coder**: `10310a7` #143 spec `.collab/design/SPEC-ephemeral-agents.md` — mode × reply-to × no-autojoin-rooms. APPROVED. Implementation starting: `--reply-to C2C_MCP_REPLY_TO`, background mode (detached daemon), headless mode (codex-headless passthrough).
- **lyra-quill**: #131 codex stdin mishandling — strong hypothesis found. Structured-input XML reader spawned as detached task with no shutdown cancellation path. Tentative fix in codex fork: keep reader JoinHandle + abort on StructuredInputRuntime drop. Can't compile-verify in sandbox (offline crossterm dep). Live repro still pending.
- **galaxy-coder**: #148 tmux location persistence in progress; pulling instance-dir access (multiple permission reqs).
- **Tasks closed (this hour)**: #120, #126, #146, #149. Still open: #107 (Max-blocked), #131 (in-flight), #132 (pending), #142 (pending), #143 (in-flight), #148 (in-flight).

## 3. Active / blocked

- **test-agent**: idle; peer-review standby + may chase the stale-binary issue they flagged from #147 testing.
- **lyra-quill**: #131 fix in codex fork (needs upstream compile-verify). #132/#142 queued.
- **jungle-coder**: #143 implementation (4 sub-items per spec).
- **galaxy-coder**: #148 implementation.
- **Stanza launch**: still gated on Max — all prereqs landed.
- **Relay deploy**: not needed; intermittent signature_invalid self-resolved.

## 4. Next actions

- **test-agent**: rest or peer-review incoming jungle/galaxy commits.
- **lyra-quill**: validate codex fork fix when compile environment is available; land + smoke.
- **jungle-coder**: start D4/D5 (reply-to + no-autojoin-rooms) as they're the cheapest.
- **galaxy-coder**: continue #148 phase 1 (capture at startup), phase 2 (display) optional.
- **Max**: launch stanza at will.

## 5. Goal tree (abbrev.)

- **Git signing**: shipped earlier today. No changes.
- **Channel-push**: stable; all peers using it.
- **Codex stability**: #113 shipped, #136 closed, #131 root cause in lyra's codex fork. #132/#142 queued.
- **Onboarding / ephemeral agents**: persistent-launch path shipped (#138/#139/#145/#146/#149). Ephemeral primitive in design-approved + early implementation (#143).
- **Swarm social**: 5 live peers; stanza imminent.
- **Instance hygiene**: `--prune-older-than` shipped. TTL auto-default investigation offered to test-agent.
- **Relay**: intermittent sig_invalid logged with diagnostic recommendation; no deploy needed.

## 6. Gaps & concerns

- **Stale-binary issue** (test-agent flagged during #147 testing) — uninvestigated; they may chase.
- **Permission-request volume**: galaxy has sent 3+ permission requests in 15 min for various instance-dir reads. `approve-always` on c2c managed dirs is appropriate and has been granted; if volume stays high, consider making `~/.local/share/c2c/instances/**` a default-allow pattern for registered peers.
- **Coordinator1 stale MCP env**: unchanged; unchanged workaround.
- **Codex fork changes** (#131 fix): live outside this tree; lyra will need to compile-verify when harness allows.
- **Sitrep discipline**: on-time this hour (:08 vs tick at :07). Peer-poll before draft worked.
