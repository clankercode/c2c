# Sitrep 02:00 UTC (coordinator1)

> **Draft metadata**
> - drafted: 2026-04-24T02:07:04Z
> - agent: `coordinator1`
> - client: claude-code
> - session: `coordinator1`
> - git HEAD: `4897638` (147 commits ahead of origin/master)
> - previous sitrep: none this day (gap — swarm was offline recovering from fork-bomb crash)

## 1. Swarm roster

| Alias        | PID     | Client     | Current focus                                 | Last activity |
|--------------|---------|------------|-----------------------------------------------|---------------|
| coordinator1 | 47042   | claude     | swarm coordination + commit hygiene           | 12:07 (this sitrep) |
| test-agent   | 225436  | opencode   | #121 (HOLD — protocol violation; awaiting Max)| 12:04 |
| Max          | —       | —          | editing `.opencode/opencode.json`, c2c_mcp.ml | 12:02 (`move debug flag`) |

Dead registrations: ~15 (sweep blocked — managed-session policy).

## 2. Recent activity (since session start ~11:00 AEST)

- Commits landed (coordinator1): a6c0582, 7ae246d, ab3a2ec, 2e22d34, 4896870, cf8d409 — findings, design docs, peer personal logs, gitignore, todo-ideas sweep
- Commits landed (test-agent): a75e883 — #127 fix (c2c_deliver_inbox ImportError)
- Commits landed (Max): 4897638 — opencode debug flag move
- Bugs closed: **#127**
- Findings filed: room-alias misattribution, shared-worktree phantom-dep (both coordinator1)
- Design docs ingested: session-id env export (galaxy), ephemeral one-shot review (tundra)
- Ideas promoted: git-proxy signing/attribution + PoW email proxy → `brainstorming and planning`
- **Incident 1 (catastrophic, earlier this session)**: `c2c git` PATH shim (da0b7a0) caused an immediate system-level fork bomb — `c2c start <client>` spawned sessions that recursively re-entered the shim (shim → `c2c git` → internal `git` → shim → …) and crashed the host. Surgical revert applied as `a23b483` via `git apply -R` (a plain `git revert` failed on pre-staged files). 4 unrelated files (`Git_helpers.ml`, `c2c.ml`, `dune`, `test_c2c_start.py`) were swept into the revert commit — Max cleared it as long as nothing was lost. Guarded redesign (recursion-guard env var + baked-in real-git path) spec handed to codex for re-implementation; current state is **no shim in tree** (#122).
- **Incident 2 (today, high severity)**: test-agent ran `git checkout HEAD -- ocaml/c2c_mcp.ml` while working on #121 — silently reset the file, potentially destroying Max's uncommitted edits (not reflog-recoverable). test-agent held, acknowledged protocol violation. Full writeup in `.collab/findings/2026-04-24T02-10-00Z-coordinator1-destructive-checkout-protocol-violation.md`. Awaiting Max's damage assessment.

## 3. Active tasks

- **coordinator1**: swarm coordination, sitreps, commit gate
- **test-agent**: **#121** (in_progress, HOLD pending Max sign-off after c2c_mcp.ml incident)
- **unassigned**: #107 (blocked on Max), #113, #122 (reimpl w/ guarded shim — codex had spec), #123-#125 (role-wizard bugs), #126 (agent refine --agent-mode)

## 4. Blocked tasks

- **Upstream/human**: #107 (Max editing c2c_mcp.ml), #121 (awaiting damage assessment)
- **S5c dedup**: duplicate `broker_ws_connect` at c2c_relay_connector.ml:560 + 797 still present; `scripts/fix_s5c.py` staged but not applied; jungle/galaxy coordination pending — both alive=null, effectively offline
- **#122 shim**: reverted, codex had spec — codex pane showed build error in c2c_mcp.ml, idle 2m+

## 5. Next actions per agent

- **coordinator1**:
  - Wait on Max's c2c_mcp.ml assessment
  - If #121 resumes: coordinate test-agent peer-review then ultrascrutiny pass (security-relevant)
  - Draft design sketch for git-proxy signing (user.signingkey auto-detect) into `.collab/design/`
  - File a findings doc for the destructive-checkout incident
- **test-agent**: hold; if cleared, resume #121 on a clean base; peer-review before commit
- **Max**: assess c2c_mcp.ml loss vs recovery (if any)

## 6. Goal tree

- **North star**: unify Claude Code, Codex, OpenCode, Kimi as first-class peers on the c2c broker
  - **[feature] agent-file epic** — v1.1 mostly live, Stage 4-7 live testing pending
  - **[feature] codex-headless** — done
  - **[feature] c2c GUI** — planned (Rust+Tauri)
  - **[flow] cross-client messaging** — working; test-agent OC↔coordinator1 CC confirmed today
  - **[sidequest] M1 mobile-pair / relay** — S5c Phase B duplicate cleanup pending
  - **[sidequest] relay health + deploys** — push gate held; no deploy warranted
  - **[sidequest] swarm social layer** — swarm-lounge active; only 2 live peers right now
  - **[sidequest] sitrep discipline** — resumed with this one after gap
  - **[sidequest] shim/attribution** — #122 reverted, guarded redesign in codex's court; git-proxy signing idea in brainstorming

## 7. Gaps & concerns

- **Swarm is thin**: only 2 live peers (coordinator1 + test-agent). jungle/galaxy/ceo/Lyra all alive=null. Productivity bottlenecked.
- **Protocol violations (two this session)**:
  1. **Fork-bomb shim (earlier)** — `c2c git` PATH shim (da0b7a0) recursively invoked itself and crashed the host on `c2c start <client>`. Reverted as `a23b483`. Root cause: shim called `c2c git`, which forks real `git`, which re-hits shim. Fix: recursion guard via env var + baked-in real git path at shim-creation time (using `Git_helpers.find_real_git ()`). Spec handed to codex; not yet re-implemented.
  2. **Destructive checkout (now)** — test-agent's `git checkout HEAD -- c2c_mcp.ml`. See `.collab/findings/…-destructive-checkout-protocol-violation.md` for systemic fix proposals.
  Both suggest the CLAUDE.md rules + memory file aren't surfacing strongly enough at the moment of execution. Proposal: surface the conflict-resolution protocol as an in-tool warning when any agent invokes destructive git ops (via the `c2c git` proxy guard once the guarded shim lands).
- **c2c_mcp.ml contention**: Max editing in parallel with agents means unstaged loss is a real risk. Consider: (a) Max works on a separate branch/worktree, (b) a pre-commit guard on c2c_mcp.ml that warns "Max may be editing", (c) periodic auto-stash→backup of uncommitted shared-file edits into `.c2c/backups/`.
- **#122 shim**: codex pane showed a build error in c2c_mcp.ml/mli — might be blocked on Max's unstaged changes being gone.
