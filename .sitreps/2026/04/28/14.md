# Sitrep 14:00 UTC (coordinator1)

> **Draft metadata**
> - drafted: 2026-04-28T14:07:07Z
> - agent: `coordinator1` (Cairn-Vigil)
> - client: claude-code
> - session: `coordinator1`
> - git HEAD: `35b886f9` (124 commits ahead of origin/master)
> - previous sitrep: [12 UTC](12.md) — 13 UTC skipped (deep in broker-root debug)

## 1. Swarm roster

| Alias         | PID     | Client   | Current focus                                                | Last activity |
|---------------|---------|----------|--------------------------------------------------------------|---------------|
| coordinator1  | 3217162 | claude   | sitrep 14 + cherry-pick stewardship + master-cliff repair    | now           |
| jungle-coder  | 3000560 | claude   | standby; #378 ack'd; #379 networking still on the bench       | <30m          |
| galaxy-coder  | 512388  | claude   | #379 S1 (relay ingress host-strip); hit pre-existing build err, fix path DM'd | <30m |
| stanza-coder  | 4675    | claude   | #379 S2 (`--relay-name` CLI plumbing) `d4b90961`             | <30m          |
| slate-coder   | 3119035 | claude   | #392b convergence + #414 + peer-PASSing test-agent #405      | <15m          |
| test-agent-oc | 2778745 | opencode | #405 crush deprecation `14fbface` — slate doing peer-PASS    | <30m          |
| birch-coder   | 2709488 | opencode | new peer (pale-wood theme); legacy broker after env override | <60m          |
| cedar-coder   | 2711042 | opencode | new peer (aromatic-cedar theme); legacy broker after env override | <60m       |
| lyra-quill    | —       | (unset)  | failover-designate; quiet                                    | unknown       |
| ceo / tundra  | —       | (unset)  | quiet                                                        | unknown       |

Dead registrations: 10 cached/non-live (sweep deferred per active-op rule).

## 2. Recent activity (12:00–14:00 UTC, 13 skipped)

**Commits landed (16 in window):**
- `35b886f9` fix(#392b): re-add lost tag helpers + xml_escape + format_c2c_envelope to c2c_mcp.ml (Cairn — master-cliff repair)
- `4a575d71` feat(#392b): migrate inbox-hook + cli postscript to format_c2c_envelope; fix extract_tag_from_content (slate)
- `1d65fdc3` fix(#383): test_c2c_mcp.ml builds — restore test wiring + 3 type annotations + drop dangling test_case (Cairn)
- `d601183f` fix(#418): get-tmux-location race + perf — pane-bound tmux query, fast-path dispatch (Cairn)
- `d04f93c1` feat(#392b): land extract_tag_from_content + xml_escape + format_c2c_envelope impls + 7 audit/research docs (Cairn)
- `b4f6a5dd` docs(client-delivery): replace ./restart-self with c2c restart in self-restart sections (#395) (Cairn)
- `cbe851c2` fix(#407 S2): smoke actually passes — relay-aware DM, working healthcheck, Dockerfile chown (slate)
- `a2a9286d` fix(#392): remove premature mli decls — impls don't exist yet on master (Cairn)
- `2d9a17bd` fix(#392): mli format_c2c_envelope signature single-line (multiline form rejected) (Cairn)
- `e31a3fb9` feat(#62): CLI verify routes through verify_claim_for_artifact (stanza)
- `27ec9201` refactor(#62 prep): extract verify_claim_for_artifact for CLI/broker convergence (stanza)
- `8d325615` fix(#392): expose extract_tag_from_content in c2c_mcp.mli (Cairn)
- `cb7c89b5` feat(#407 S2): cross-broker DM smoke — fixes S1 healthcheck + adds modes (Cairn)
- `e6a8fb62` chore: archive batches — collab-updates pre-04-22 + today's resolved findings (Cairn)
- `a44db752` feat(#407 S1): e2e Docker baseline — promote compose + smoke drafts to active (Cairn)
- `c9a33d15` scripts(cc-quota): comment out cost line per Max preference (Cairn)
- (sitrep 12 commit `dad2160d` outside the window for #2 but is the previous-link anchor)
- (slate's `d4b90961` `--relay-name` plumbing landed in this window via stanza for #379 S2)

**Hour highlights — pivotal swarm composition:**
- **Two new OpenCode peers spun up**: `birch-coder` (pale-wood theme, 0:3.1) + `cedar-coder` (aromatic-cedar theme, 0:3.5). Roles at `.c2c/roles/{birch,cedar}-coder.md`. Now on legacy broker after a long broker-root debug saga.
- **Broker-root mismatch saga (root-caused)**: birch+cedar initially registered to canonical `$HOME/.c2c/repos/<fp>/broker` while live swarm runs on legacy `.git/c2c/mcp` (env-var override on every peer). Diagnosis: `c2c start` reads `broker_root` from persisted instance config (`~/.local/share/c2c/instances/<name>/config.json`) and **ignores** `C2C_MCP_BROKER_ROOT` env var when both differ. Manual config rewrite + restart unblocked them. Filed **#422** (initial OC plugin theory, since revised) and **#424** (the actual fix: env var should override persisted-config when present).
- **Master-cliff fixes (Cairn)**: re-added lost `tag_to_body_prefix` / `parse_send_tag` / `extract_tag_from_content` / `format_c2c_envelope` impls + `xml_escape` to `c2c_mcp.ml` after several rounds of working-tree state loss. Test-runner wiring repair at `1d65fdc3` (3 type annotations + dangling `test_case` ref removal). Build green again.
- **Slate's parallel fan-out**: shipped **#392b** (envelope-attribute formatter convergence, inbox-hook + CLI postscript both routed through `format_c2c_envelope`) and **#414** (author-aliases config + verbatim-recipe rule). Day-1+1 totals approaching **17 PASSes**.
- **Stanza on #379 S2**: `d4b90961` `--relay-name` CLI plumbing landed; #62 CLI verify-claim refactor also progressed (`27ec9201` + `e31a3fb9`).
- **Galaxy on #379 S1**: relay ingress host-strip; ran into pre-existing `test_c2c_mcp.ml` `msg.content` build error which Cairn had already fixed at `1d65fdc3` — DM'd her the fix path.
- **Test-agent #405 crush deprecation** ready at `14fbface`; slate doing peer-PASS.
- **Gemini auth context (Max)**: install + login complete locally; e2e tests should bind-mount `~/.gemini/` and use `gemini-3.1-flash-lite-preview`. Filed **#416**.

**Findings filed (this window)**: 7 audit/research docs landed with `d04f93c1` (#392b context). Broker-root saga findings to be written up next turn.

**Tasks filed (#418-#424)**: get-tmux-location race+perf (#418, fixed `d601183f`), lean-init audit (#419), role-file autoload (#420), resume command UUID (#421), OC plugin broker-env theory (#422 — superseded), persisted-config override fix (#423-#424), TUI role-creation inadequacy.

## 3. Active tasks

- **coordinator1**: sitrep 14; cherry-pick stewardship; broker-root finding write-up; assess push readiness once dust settles.
- **stanza-coder**: #379 S2 `--relay-name` plumbing landed; pick S3 or pause for Max.
- **galaxy-coder**: #379 S1 relay ingress host-strip — unblocked by Cairn's `1d65fdc3` fix path.
- **slate-coder**: peer-PASS on test-agent's #405 (`14fbface`); #392b cleanup + #414 ripple.
- **test-agent-oc**: #405 crush deprecation in slate's review queue.
- **birch-coder + cedar-coder**: orienting; legacy broker confirmed; first slices TBD by coord.

## 4. Blocked tasks

- **External (build)**: galaxy's #379 S1 hit pre-existing `msg.content` error → Cairn DM'd path to `1d65fdc3` fix; should be unblocked.
- **Human**: #379 cross-host FINISH path — still awaiting Max go/no-go for the full 4-8hr commit beyond S1+S2.
- **Review**: test-agent's #405 (`14fbface`) — slate active.
- **Push gate**: **124 commits ahead of origin/master** (up from 107 at 12 UTC, +17 in 2 hours). Master-cliff just restored, so wait for one clean turn before assessing. `c2c doctor` next quiet pocket.
- **Config bug**: #424 persisted-config-vs-env-var precedence — onboarding-blocker class for new peers; needs prioritization.

## 5. Next actions per agent

- **coordinator1**: write up broker-root saga finding; run `c2c doctor` for push verdict (124 is well past comfortable batch); restock idle peers; orient birch+cedar with first slices.
- **stanza-coder**: continue #379 S3 if Max greenlights, else pick from #62 follow-on or #423/#424 config-precedence fix.
- **galaxy-coder**: confirm `1d65fdc3` unblocked the #379 S1 build; commit + handoff for peer-PASS.
- **slate-coder**: complete peer-PASS on test-agent's #405; pick from #418-cluster cleanup or back to #407 e2e shards.
- **test-agent-oc**: standby for slate's PASS verdict on #405; if FAIL, fix in new commit; if PASS, pick next from #407 cluster.
- **birch-coder**: orient via `.collab/runbooks/first-5-turns-for-new-agents.md`; first dispatch from coord (likely a small surface-doc or test-fixture slice).
- **cedar-coder**: same orientation; first dispatch likely a paired-class slice with birch for early socialization.
- **jungle-coder**: standby — networking class still warm; #379 S3 or e2e Docker (#407) if greenlit.

## 6. Goal tree

- **North star**: unify Claude / Codex / OpenCode / Kimi / Gemini as first-class c2c peers.
  - **[feature] delivery surfaces** — MCP + CLI + #341 restart-intro live; **#392b** envelope-formatter convergence sealed.
  - **[feature] reach** — 5/5 clients first-class; **#405 crush deprecation** in peer-PASS.
  - **[feature] topology** — 1:1 ✓, 1:N ✓, N:N ✓ (rooms + ACLs).
  - **[feature] social layer** — `swarm-lounge` healthy; **birch + cedar onboarded** today (8 live peers, peak roster).
  - **[infra] peer-pass crypto** — sealed; **#62 CLI/broker convergence** in flight (stanza).
  - **[infra] broker-root resolution** — **#422/#423/#424** surfaced today; persisted-config-vs-env precedence is the real bug. Onboarding-blocker class.
  - **[infra] auto-DM author resolution** — #414 sealed.
  - **[infra] tmux helper perf** — **#418 sealed** (`d601183f` race + perf fix).
  - **[future] cross-host topology / #379** — S1 (galaxy) + S2 (stanza, `d4b90961`) landing; FINISH still gated on Max.
  - **[future] gemini adapter** — #416 filed (auth + e2e bind-mount + model spec).
  - **[sidequest] e2e Docker (#407)** — S1 (`a44db752`) + S2 (`cbe851c2`) landed; remaining shards open.
  - **[sidequest] role-file ergonomics** — #420 (autoload), #421 (resume UUID), TUI role-creation inadequacy filed.
  - **[sidequest] sitrep discipline** — 14 done; 13 deliberately skipped (broker-root debug); next at 15 UTC.

## 7. Gaps & concerns

- **Broker-root config precedence (#424) is an onboarding cliff.** Two new peers spent ~an hour invisible to the swarm because env-var override silently lost to persisted instance config. This is the same shape as #340 / #373 — "obvious" config wins were footguns. Fix should be coord-prioritized; affects every future new peer until landed.
- **Master-cliff oscillation**: lost `format_c2c_envelope` / `extract_tag_from_content` / `xml_escape` impls happened across multiple working-tree state losses today. Suggests #392b's mli-first landing pattern is fragile under cherry-pick chains. Worth a runbook entry on "land impl before mli" or a CI guard.
- **124 commits ahead — push assessment overdue.** +17 since 12 UTC. Will run `c2c doctor` once master is stable for a full turn. Security trio + #392b + #414 + #383 are user-facing wins worth deploying.
- **Quota at 49% with 1h54m to reset** — target band 95-99%. Headroom for one more dispatch wave; pace check at next sitrep.
- **birch + cedar still orienting** — first dispatch must be small enough to validate end-to-end (register → poll → send → archive) before committing them to a real slice. Otherwise a config bug masquerades as a content bug.
- **13 UTC sitrep skipped** — broker-root debug saga consumed the hour. Documented here as the gap; resume normal cadence at 15 UTC.

— Cairn-Vigil
