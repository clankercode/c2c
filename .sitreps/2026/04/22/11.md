# Sitrep 11:00 UTC (coordinator1)

> **Draft metadata**
> - drafted: 2026-04-22T11:13:43Z
> - agent: `coordinator1`
> - client: claude
> - session: `coordinator1`
> - git HEAD: `b1461a9` (41 commits ahead of origin/master)
> - previous sitrep: [10 UTC](10.md)

## 1. Swarm roster

| Alias        | PID      | Client   | Current focus                                    | Last activity |
|--------------|----------|----------|--------------------------------------------------|---------------|
| coordinator1 | 3159517  | claude   | hourly sitrep + dispatch                         | now           |
| ceo          | 3055603  | opencode | M2/M4 tests shipped; next slice TBD              | ~5m ago       |
| galaxy-coder | 3981... (post-restart) | opencode | compacting-status end-to-end dogfood test       | ~1m ago       |
| jungel-coder | null (registered, alive per DMs) | opencode | reply_via envelope — design starting | ~1m ago       |

Dead registrations: ~10 stale test aliases unchanged. `jungle-coder` (no L) dead; `jungel-coder` (L) alive. Alias-typo finding filed by ceo this hour.

## 2. Recent activity (10:00–11:00 UTC)

Biggest hour yet. Security epic went from blockers-pending to end-to-end delivered:

- **M3 plugin-side supervisor-list verification** (ceo, `6e4c671`): the second, correct attempt — first one (`15713e9`) was architecturally flawed and got reverted in `3f2d852`.
- **M2 broker pending-permission registry** (ceo, `9eb17a0`): new `pending_permission` type, RPCs `open_pending_reply`/`check_pending_reply`, lazy TTL eviction, helpers. 151/151 OCaml tests green.
- **M2 plugin-side RPC wire-up** (jungle-coder, `d116139`): c2c.ts calls `open-pending-reply` before send, `check-pending-reply` before resolving reply. Advisory fallthrough preserves old-plugin compatibility. 155/155 tests green.
- **M4 alias-reuse guard** (ceo, bundled in `9eb17a0`): `register` refuses if prior owner alive and has pending state.
- **M2/M4 dedicated tests** (ceo, `784dc0d`): direct coverage for the new RPCs.
- **Compacting-status feature** (galaxy-coder, `f0f31ed`): registration schema `compacting: {started_at, reason?}`; `c2c send` appends warning envelope; 5min stale-flag guard; MCP tools `set_compacting`/`clear_compacting`.
- **Compacting-status hook chain** (galaxy-coder, in-flight): `~/.claude/hooks/c2c-precompact.sh` + settings.json + CLI `c2c set-compact`/`c2c clear-compact` + MCP handlers — all wired.
- **OCaml layout-parser bug in set_compacting handlers** (jungle-coder filed; galaxy-coder fixed in 784dc0d): inner match in match-arm needed parens. Peer-to-peer resolution, coordinator hands-off.
- **c2c_tmux enhancement** (coordinator1, `e6d0c77`): alias cache + target-on-action banners; writes refused to cached targets (so a restart doesn't clobber a reused pane).
- **reply_via envelope field** (Max bug, `4e1ce50`; was `0335e64` orphaned by rebase): explicit `reply_via` attribute on `<c2c>` envelopes. jungle-coder now designing.
- **Deprecated-script cleanup** promoted to long-term goal (`b1461a9`).
- **Findings**: permission-reply-didn't-unstick-TUI (coordinator1, 20-27); jungel-vs-jungle alias typo (ceo, 20-05 + 20-00); set_compacting type error (jungle-coder, 21-03).
- **Extensive terminal-E2E test hardening** (jungle-coder, multiple commits): fake-pty driver, tmux driver launch contract, scenario helpers, codex twin e2e.
- **Operator intervention** (once): coordinator1 pressed enter in galaxy-coder's pane to unstick a permission TUI after stale-plugin rejected a legit supervisor reply.

## 3. Active tasks

- **coordinator1**: this sitrep, dispatch, admin.
- **galaxy-coder**: live-dogfood compacting-status end-to-end — `c2c set-compact` then expect warning envelope on inbound DM.
- **ceo**: next slice TBD.
- **jungle-coder**: reply_via envelope design doc.

## 4. Blocked tasks

- **Stage 4–7 agent-file live testing** (Max-blocked, unchanged).
- **Codex-headless 3–5, 7**: reserved for the codex node per Max's directive.
- **release-manager role stub**: unassigned; low priority.

## 5. Next actions per agent

- **coordinator1**: commit sitrep, dispatch, keep heartbeat cadence.
- **galaxy-coder**: finish compacting-status live-dogfood test; report with a positive finding; then pick a next slice (OpenCode /compact wrapper would be a natural parallel).
- **ceo**: options — (a) docs page for pending-permission RPC flow; (b) plugin-side test coverage for M2/M4 (pair with jungle-coder on the existing TS tests); (c) respond to jungle-coder's reply_via design doc when it lands. Coordinator suggests (a) first.
- **jungle-coder**: draft `.collab/design/envelope-reply-via.md` — inventory current envelopes, proposed attribute, backward-compat default (`c2c_send` for DMs), scope (DMs v1), room-extension path (`send_room:<id>`). Then small impl slice.

## 6. Goal tree

- **North star**: unify Claude Code, Codex, OpenCode, Kimi as first-class
  peers on the c2c broker
  - **[feature] agent-file epic** — v1.1 mostly done; Stage 4–7 human-blocked
  - **[feature] security: permission/question alias-hijack** — **M2 / M3 / M4 ALL LANDED THIS HOUR**
    - Remaining: observability polish, live wild dogfood, deployment decision
  - **[feature] compacting-status on statefiles** — chain complete end-to-end
    - Verification via dogfood in progress
    - [sidequest] OpenCode /compact wrapper (unassigned)
  - **[feature] c2c envelope reply_via** — design in flight
  - **[feature] codex-headless** — reserved for the codex node
  - **[feature] c2c GUI** — dormant this hour
  - **[feature] remote reach (horizon)** — relay security largely hardened now
  - **[flow] cross-client permission routing** — hardened end-to-end this hour
  - **[sidequest] relay health + deploys** — 41 commits ahead; still no relay change
  - **[sidequest] deprecated-script cleanup** — long-term goal
  - **[sidequest] sitrep discipline** — this file

## 7. Gaps & concerns

- **41 commits ahead of origin**. Still no relay/website change in the queue, so no deploy trigger. `c2c doctor` check would confirm.
- **Monitor idle-batch** dumped 15+-min-old swarm-lounge messages twice this hour. ceo's finding stands; still no short-term fix beyond ignoring batches.
- **Alias typo persists**: both findings filed, dedup plan unexecuted. Low-priority swarm-UX noise.
- **Orphaned commits from rebases**: my `0335e64` silently dropped. Re-committed as `4e1ce50`. Investigate next hour — who is rebasing what? A silent loss of committed work is a real defect.
- **Compacting-status dogfood verdict** pending as of sitrep commit. If the live test fails, the hooks + handlers are paper-wired; galaxy-coder will surface either way.
- **`.projects/<name>/` convention** still unused; `todo-ongoing.txt` summaries carry everything. Not urgent but worth creating on-demand as projects grow.
- **Observability/plugin-version probe** is the natural next security slice — the permission-reply-didn't-unstick-TUI finding proposed 5 mitigations, none yet picked up.
