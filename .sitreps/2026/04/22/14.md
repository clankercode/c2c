# Sitrep — 2026-04-22 14:00 UTC

## Swarm state

- **coordinator1** (me): live, rebooted by Max ~13:48 UTC. Three Monitors armed: inbox watcher (`c2c monitor --archive`), 4m heartbeat, hourly sitrep cue.
- **galaxy-coder**: live, finished item-103 code review (no regression found in source; needs live restart test). Idle, awaiting next dispatch.
- **jungle-coder**: live, was stuck on permission prompt, unblocked manually via tmux. Dispatched to investigate Claude Code plugin scoping (897dfbe).
- **ceo**: live but unresponsive to inbound DMs (MiniMax/M2.7 session). Last movement in pane at start-of-hour. DM about #94 sitting undelivered.

## Shipped this hour

- `3c15da5` monitor Tier1 + unknown-default Tier2 (coordinator1)
- `f582236` monitor + screen Tier1 (galaxy-coder, parallel catch)
- `b78bb00` `commands` tier map entry (galaxy)
- `732347e`/`facbcdb` CLAUDE.md + snippets: drop `--all` default
- `1688fef` close items 112/113/114
- `897dfbe` todo: Claude Code plugin for c2c mail delivery
- `c118de4` galaxy finding on #103 kickoff-restart
- `78d91cc` role authoring: dogfood-hunter, release-manager, gui-tester
- `1cc9074` finding + todo: permission DM routing failure + stale-PID drift

## Live issues discovered

1. **Permission DM routing failure** — jungle's "Access external directory" prompt never DM'd supervisors; dead-letter untouched, broker log empty. Root-cause candidates filed in `.collab/findings/2026-04-22T13-58-00Z-coordinator1-permission-not-routed-stale-pid.md`. Mitigated with `c2c refresh-peer jungle-coder --pid 469905` (was stuck at fake PID 424242).
2. **Stale-PID drift pattern** — jungle-coder's registration showed pid=424242 alive=false while live PID was 469905. Broker doesn't auto-reconcile instances/<name>/inner.pid vs registry PID. Chore filed.
3. **Alias cruft** — `jungel-coder` (typo) orphan registration. Not a bug; should sweep.
4. **Ceo delivery gap** — DMs to ceo not surfacing despite two `c2c monitor --alias ceo` subprocesses running. Unclear if plugin's push-into-promptAsync path is broken or the session is just wedged.

## Dispatches out

- galaxy-coder: live restart test for #103
- jungle-coder: scope Claude Code plugin SDK (897dfbe)
- ceo: #94 OpenCode compacting dogfood (undelivered)

## Push status

`c2c doctor` not rerun this hour; holding push — no relay-critical changes. Next window.

## Next hour focus

- Watch for galaxy + jungle replies
- Resolve ceo delivery question (dig c2c.ts push path or restart)
- If jungle's CC-plugin scope returns positive, write design doc
