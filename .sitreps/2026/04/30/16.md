# Sitrep 16:00 UTC (coordinator1)

> **Draft metadata**
> - drafted: 2026-04-30T16:08:57Z
> - agent: `coordinator1` (Cairn-Vigil)
> - client: claude
> - session: `coordinator1`
> - git HEAD: `a6212205` (86 commits ahead of origin/master)
> - previous sitrep: [09 UTC](09.md)

## 1. Swarm roster

| Alias            | PID      | Client    | Current focus | Last activity |
|------------------|----------|-----------|---------------|---------------|
| coordinator1     | 2625926  | claude    | sitrep / cherry-pick wave | active |
| jungle-coder     | 919816   | claude    | #511 Slice 1 (authorizer resolver) | active |
| cedar-coder      | 1363192  | claude    | #510 git-shim install + #511 S3 (blocked) | active |
| galaxy-coder     | 919975   | claude    | #407 cross-host mesh | active |
| fern-coder       | 1158214  | claude    | #473 kimi quick-ref + reviewer | active |
| birch-coder      | 1082659  | claude    | post-#509 cleanup | active |
| stanza-coder     | 917665   | claude    | #450 handle_tool_call extraction | active |
| test-agent       | 920289   | claude    | #511 Slice 2 (blocked on S1) | waiting |
| willow-coder     | 1147557  | claude    | #491 codex_resume_target wire-in | active |
| lumi-tyyni       | n/a      | kimi      | first-class kimi peer | bring-up |
| tyyni-sora       | n/a      | kimi      | first-class kimi peer | bring-up |

Dead registrations: 3 (smoke-opencode×2, slate-coder — sweep deferred per CLAUDE.md).

## 2. Recent activity (since prior sitrep)

- **Commits landed (5)**:
  - `73cfb6ee` fix(#518): trim empty-string `C2C_MCP_BROKER_ROOT` in non-resolver call sites
  - `24111ed5` (#507) sidecar broker_root_fingerprint + `--sync-sidecar` flag
  - `cf4c53fe` (#506) embed broker_root in pending JSON + `--broker-root` flag on approval-reply
  - `1a3e8f88` docs(#161) clarify kimi `^Bash$:` matcher syntax
  - `72bd42c7` (#502) warn-on-use deprecation for `C2C_KIMI_APPROVAL_REVIEWER`
  - `a6212205` feat(#512): `c2c migrate-broker --rewrite-mcp-configs` ← this sitrep window
- **Bugs closed**: #518, #514, #512, #478 (already-applied), #506, #507, #502, #161 docs
- **Findings filed**: empty-string env-var fall-through (#518) confirmed root cause of CWD broker pollution
- **Design docs updated**: `.collab/design/2026-05-01-prompt-forwarding-fallback-authorizers.md` (jungle, #511)
- **Permissions / broker events**: post-compact restart of coord1; channel push verified working;
  MCP confirmed on canonical broker; 5 Monitors re-armed.

## 3. Active tasks

- **coordinator1**: cherry-pick wave (done), 16 UTC sitrep, prep dispatch for #517 + #511 cascade
- **jungle-coder**: #511 Slice 1 (authorizer resolver — `c2c_repo.ml` schema parse + walk)
- **stanza-coder**: #450 handle_tool_call extraction (queued)
- **cedar-coder**: #510 git-shim wire-in (in-progress)
- **galaxy-coder**: #407 cross-host mesh (in-progress)
- **willow-coder**: #491 codex_resume_target → prepare_launch_args (in-progress)
- **fern-coder**: #473 Kimi-as-peer operator quick-ref (pending)

## 4. Blocked tasks

- **#511 Slice 2** (test-agent): blocked-by S1 SHA from jungle
- **#511 Slice 3** (cedar): blocked-by S1+S2
- **#513**: pre-push cleanup of cedar shim-test stray commits — needs interactive rebase by coord
- **#473** (fern): pending — fern hasn't claimed; nudge after sitrep
- **#516**: this sitrep itself (now in flight)

## 5. Next actions per agent

- **coordinator1**:
  - send dispatch for #517 (`c2c list` → tmux session:window.pane) to a free hand
  - nudge fern on #473 + ask about #511 capacity for an authorizer slot
  - watch for jungle's S1 SHA → unblock test-agent immediately
  - audit `.worktrees/` for anything stale; run `c2c worktree gc --dry-run`
- **jungle-coder**: complete #511 S1 — schema parse + `resolve_authorizer_chain()` + tests in
  `test_c2c_repo.ml`; DM SHA when ready.
- **test-agent**: **HOLD** until S1 lands. Use idle for #491 review or pick up #517.
- **cedar-coder**: keep moving on #510; #511 S3 stays parked.
- **galaxy-coder**: progress check on #407 — request status DM.
- **willow-coder**: #491 keep moving; ETA?
- **fern-coder**: #473 — confirm claim or pass; #511 capacity for Slice 4 (pending-record schema).
- **stanza-coder**: #450 handle_tool_call — texture-honest "what's the next blocker?" if stuck.

## 6. Goal tree

- **North star**: unify Claude Code, Codex, OpenCode, Kimi as first-class peers on the c2c broker
  - **[delivery surface] permission/approval flows**
    - #511 prompt forwarding + fallback authorizers (jungle, in-flight S1; test-agent S2; cedar S3)
    - #490 approval side-channel (multi-slice arc; #506 + #507 landed; #511 supersedes #142)
    - #493 operator-discipline runbook + legacy-format deprecation (pending)
  - **[delivery surface] CLI self-configuration**
    - #510 git-shim auto-install (cedar, in-flight)
    - #517 `c2c list` shows tmux pane (pending dispatch)
  - **[reach] kimi as first-class peer**
    - #473 operator quick-ref (fern, pending)
    - #484 MCP-strengthening for `c2c await-reply` (pending; cedar candidate)
    - kimi hook approval flow — partially done via #511
  - **[topology] cross-host mesh**
    - #407 e2e Docker suite expansion (galaxy, in-flight)
    - #515 cross-host mesh status check (galaxy, gating)
  - **[social/relational]**
    - swarm-lounge stable; per-agent memory tier in active use
  - **[sidequest] broker robustness post-migration**
    - #501 (executed), #503 (resolver fallthrough), #505 (drift), #518 (env empty), #512 (mcp-config rewriter) — full cluster CLOSED

## 7. Gaps & concerns

- **#473 unclaimed for ~hours.** Fern needs nudge; if it's not happening, reassign.
- **#511 critical path on jungle alone.** If jungle stalls, S1 is the chokepoint. Consider asking
  fern or birch to shadow the resolver design so we can swap if needed.
- **`.mcp.json` and `scripts/mesh_test_client.py` still in `M` state in main tree** (untracked
  design doc too). Not from active work — pre-compact residue. Should be either committed or reset
  before next sitrep to keep the tree clean.
- **3 stale `c2c monitor --alias` processes still on box** (#518 follow-up). They hold the buggy
  pre-73cfb6ee binary. Decision to kill deferred at restart per Max's "Hmm okay" — re-evaluate
  when post-restart smoke confirms no regressions.
- **Dead registrations** (smoke-opencode×2, slate-coder): sweep would drop them but rule says no
  sweep during active swarm; revisit when truly idle.
- **Cherry-pick window overlap**: #507 + #512 both touched `migrate_broker_run` signature. The
  predictable shape stanza flagged. Dune file remains a known merge hotspot; no per-slice fix
  yet — Pattern 5 in worktree-discipline runbook is the doc for it.
