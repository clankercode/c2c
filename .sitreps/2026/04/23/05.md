# Sitrep 05:00 UTC (coordinator1)

> **Draft metadata**
> - drafted: 2026-04-23T05:07:03Z
> - agent: `coordinator1` (Cairn-Vigil)
> - client: claude-code
> - session: coordinator1
> - git HEAD: `6dedd74` (80 commits ahead of origin/master)
> - previous sitrep: [04 UTC](04.md)

## 1. Swarm roster

| Alias          | PID     | Client       | Current focus                                | Last activity |
|----------------|---------|--------------|----------------------------------------------|---------------|
| coordinator1   | 1356245 | claude-code  | M1 review gate, pmodel, relay audit triage   | active        |
| galaxy-coder   | 3437794 | claude-code  | M1-S3 E2E send/receive wiring + TOFU/downgrade | active (efe4af9, 6dedd74) |
| jungle-coder   | 971193  | claude-code  | M1-S2 rework (signed pubkey endpoint)        | active (InMemoryRelay stubs landed) |
| Lyra-Quill     | —       | unknown      | —                                            | idle this hour |
| ceo            | —       | unknown      | —                                            | idle this hour |

Dead registrations: ~8 test aliases (oc-*, kimi-wire-*, *-test) — not live, safe to ignore.

## 2. Recent activity (since prior sitrep 04 UTC)

- **Commits landed** (last hour, master): 9
  - `6dedd74` (galaxy) feat(S3): add `enc_status` to message type + construction sites
  - `832dc72` (me) docs(reviews): structured relay audit — 6 parallel reviewers + synthesis
  - `be7c8cd` sitreps: rewrite view-latest using rich library
  - `efe4af9` (galaxy) feat(S3): Broker ephemeral E2E state — TOFU `known_keys` + `downgrade_detection` + identity helper
  - `7b6a7a7` (me) fix(pmodel): export `pmodel` type + parser/lookup in `c2c_start.mli` (review-and-fix follow-up)
  - `2f9272a` fix view-latest: table state machine, cell indexing, bold colorization
  - `e31f557` (subagent) feat(config): `[pmodel]` provider:model preferences + resolver chain
  - `9a22299` sitreps: add view-latest colorized sitrep viewer
  - (plus jungle's WIP InMemoryRelay stubs — committed in the window, build green)
- **Reviews dispatched**: 2 (pmodel `e31f557` → FAIL→fix→PASS, galaxy S3 `efe4af9` → PASS with 3 follow-up items flagged)
- **Findings filed**: 1 (`2026-04-23T04-35-00Z-galaxy-coder-dune-revert-issue.md` — galaxy) + `2026-04-23T16-37-52Z-codex-problems-log.md` (untracked, codex)
- **Design docs updated**: relay audit dir (`.collab/reviews/2026-04-23-relay/` — 8 files, 1538 lines committed as 832dc72)
- **Permissions / notable broker events**: none this hour

## 3. Active tasks

- **coordinator1**: review gate on M1 commits; relay-audit synthesis awaiting Max's go on p0 landings; pmodel slice complete
- **jungle-coder**: M1-S2 rework — had to add `enc_pubkey_of`/`signed_at_of`/`signature_of` to `InMemoryRelay` to satisfy the RELAY sig (done, build green). S2 endpoint still owes signed_at + signature columns per prior review FAIL
- **galaxy-coder**: M1-S3 — shipped `enc_status` field + TOFU Hashtbls + identity persistence helpers. Next: wire `decide_enc_status` into MCP send/receive + TOFU compare-and-reject + `Lwt_mutex.t` around check-then-set
- **ceo**: idle this hour (pinged earlier, no claim)

## 4. Blocked tasks

- **Upstream**:
  - M1-S4 (observer WS) blocked on S2 fully landing (jungle)
  - M1-S5a (QR pairing) blocked on S4
- **Human**:
  - Relay audit p0 findings (5 security, 6 correctness, 5 api-design) — synthesis.md ready, need Max's go/no-go on phased landing (file tasks / land one-liners / hold)
  - pmodel resolver consumption: Max to pick first wire target (`c2c start` env injection vs `c2c roles compile` emit)
- **External dep**: none
- **Review**: none outstanding; both commits this hour reviewed

## 5. Next actions per agent

- **coordinator1**: (1) DM jungle to close S2 — add signed_at+signature columns per prior FAIL review. (2) Peek ceo/Lyra panes — offer relay-audit tasks if idle. (3) Watch S3 wiring commit from galaxy; review on arrival.
- **jungle-coder**: Close S2 fix-pass (columns + return shape + alias validation regex + test_relay_pubkey.ml). Then pick up S4 (observer WS — manual RFC 6455 on Conduit flow).
- **galaxy-coder**: Wire TOFU compare-and-reject + `decide_enc_status` into MCP send/receive deliver path with `Lwt_mutex.t` around pin operations. Flagged in DM.
- **ceo**: No active thread. Available for relay audit quick-wins if interested (observability logs, /metrics endpoint — both small, high-value).

## 6. Goal tree

- **North star**: unify Claude Code, Codex, OpenCode, Kimi as first-class peers on the c2c broker
  - **[epic] c2c mobile app** (active — M1 in flight)
    - S1 register X25519 ✓
    - S2 signed pubkey endpoint (jungle, fix-pass open)
    - S3 opportunistic E2E (galaxy, stub state landed, wiring next)
    - S4 observer WS · S4b rate-limit ✓ · S5a/b QR + OAuth · S6 short-queue · S7 contract tests · S8 docs
  - **[sidequest] relay audit** (synthesis ready; awaiting Max go)
  - **[sidequest] pmodel preferences** (landed; launcher wiring follow-up open)
  - **[sidequest] swarm social layer** (quiet hour; swarm-lounge idle)
  - **[sidequest] sitrep discipline** (view-latest viewer shipped this hour)

## 7. Gaps & concerns

- **Empty-file pollution**: found stray `None`/`Some` zero-byte files at repo root this hour (removed). Likely a subagent shell redirect to `$x` where `x` was an OCaml option. Low-severity but worth flagging.
- **Pmodel is advisory only**: `resolve_pmodel` lands but nothing consumes it at launch time yet. Three questions for Max pending (return type, first consumer, role_class backfill).
- **Dead test aliases accumulating** in registry (~8). Scheduling a targeted sweep next sitrep if they're still stale.
- **Peer `git add -A` footguns remain live**: I lost one docs commit to a peer sweep yesterday. Raised in CLAUDE.md; still worth a live reminder when peers go quiet.

## 8. Outstanding issues for Max

1. **Relay audit p0 landings** — synthesis at `.collab/reviews/2026-04-23-relay/synthesis.md`. 5 security (DM spoofing, SQLite TOFU bypass, path traversal, non-constant-time bearer, rate-limiter no coverage), 6 correctness, 5 api-design. a) file tasks #96+, b) land safe one-liners now, c) hold until M1 ships.
2. **pmodel first consumer** — `c2c start` env injection, or `c2c roles compile` emit `model:` per-client? Also: resolver return raw string vs parsed record.
3. **`role_class:` backfill** in `.c2c/roles/builtins/*.md` — this change or follow-up?
