# Sitrep — 2026-04-25 17:00 UTC+10

## Master tip
- `00cd102` (deferrable bug finding) → builds + installed.
- Range since 16:00 sitrep: 0f00901 → 91d0cdd → 0f00901 → 163228d → fb5c022 → 80dd6d0 → 09b6f53 → 3cb487f → 06fb6de → fe2bbad → ee46b34 → 00cd102 (a busy hour).

## Shipped
- **#107 Slice 1 + fix** (jungle, peer-PASS by stanza). Role field on registration + register tool param + JSON serialization round-trip.
- **#132 Tasks 1-3** (galaxy, peer-PASS by lyra). `parse_managed_server_request_event` + `forward_permission_to_supervisors` + bridge-events FIFO wiring into `codex-headless`. 16 tests passing.
- **#167 ALL THREE SLICES** (stanza, coord-as-peer-PASS + self-PASS smoke). OpenCode agent/mode preserved across c2c message delivery via `C2C_AGENT_NAME` env var → `body.agent` in promptAsync. End-to-end smoke through dogfood-hunter as launched agent confirmed mode preservation.
- **#165 main-guard** (stanza). `c2c start --worktree` refuses both `master` and `main`.
- **doctor warn-on-shared-tree** (stanza). Flags main tree on slice branches.
- **doctor warn-on-uncommitted-WIP** (stanza). Flags uncommitted modifications in main tree.
- **Sitrep 16:00** (coordinator1). Logged the hour.
- **Findings: deferrable=true ignored on push** (coordinator1, after dogfood-hunter live test).

## In flight
- **#107 Slice 2** (jungle). Channel notification + emit_notification with role attribute. Build PASS, tests green, restarted via `c2c restart jungle-coder` (first dogfooding of the inner-pid signal fix from earlier this session — worked, no pane drift). Awaiting his post-restart verification.
- **#132 Task 4** (galaxy). Synthetic-event smoke probe in motion — has been DM'ing me real-shaped permission requests and I've been approving. The forward_permission_to_supervisors path is provably live (multiple permission DMs received via channel notification). codex-headless FIFO deadlock blocks the *codex-headless-driven* smoke, but synthetic-event smoke is the proof. Galaxy fixed a `_find_c2c_binary` bug en-route (was returning Python shim instead of OCaml binary).

## New peer
- **dogfood-hunter** (Max launch, ~17:00). Scope: delivery testing + bug finding. Already exercising CLI/MCP round-trips, room broadcast, and deferrable-flag semantics. Filed #178 from his test results.

## Idle / unassigned
- **stanza**: idle post-#167 trio. Available for plugin-reset structural fix (#179) or new slice.
- **test-agent**: AFK ~90min through 4 pings. Treating as offline.
- **lyra**: pairing with galaxy on #132; also been emitting healthy room chatter.
- **jungle**: in restart cycle for #107 Slice 2 verification.

## Lessons / friction
- **Branch checkouts in shared main tree clobber other agents' working-tree files.** Stanza's plugin file regressed 3x — root cause: galaxy was checking out slice/132 in main tree, flipping `.opencode/plugins/c2c.ts` to that branch's version. Solved by DM'ing galaxy to migrate to his worktree (`.c2c/worktrees/galaxy-coder/` already exists). Filed #179 for structural fix (treat plugin as build artifact written by `c2c start`).
- **Stale-binary-after-OCaml-merge.** `just install-all` doesn't run automatically on merge to master; agents using the installed binary get the old behavior until someone runs install. Stanza had to apply her own change to working tree + manual rebuild during smoke. Possible doctor-warn surface: "installed binary older than master tip".
- **Nested-session permission boundary blocks live probes.** Galaxy couldn't run `c2c start codex` from inside his own c2c session for #132 Task 4 live probe; synthetic-event approach is the workaround.
- **`c2c restart` fix from earlier today verified in production.** First real use was jungle's restart at ~17:00. Worked, no pane drift. The early-session diagnosis-and-ship loop closed cleanly.
- **deferrable=true flag bug.** First flagged via dogfood-hunter live test — push happens regardless. Filed as #178.
- **Multiple smoke-test signals confused with each other.** Galaxy's first permission-forward DM at 16:45 was real, and used as live evidence; later galaxy explicitly framed his sends as smoke. Worth distinguishing in future.

## Open tasks (pending)
- #107 (Slice 2 in flight, Slice 3 envelope-emit if needed)
- #132 (Task 4 smoke completing)
- #177 codex-headless FIFO deadlock (newly filed, unowned)
- #178 deferrable-bug (filed, unowned)
- #179 plugin-as-build-artifact (filed, unowned)

## Push status
- Master 100+ commits ahead of `origin/master`. No relay-critical changes since #135.
- #132 Tasks 1-3 are deliver-daemon + start-launcher changes — not relay-touching, but they do affect every managed Codex session. Once Task 4 smoke + lyra's full peer-PASS lands, this is push-worthy.
