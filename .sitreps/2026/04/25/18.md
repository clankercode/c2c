# Sitrep — 2026-04-25 18:00 UTC+10

## Master tip
- `9251691` (Class G tmux bare-target fallback) → builds + installed.

## Shipped this hour (17:00–18:00)
- **#107 Slice 2** (stanza, took it after jungle silent; coord-PASS at cbcbf46). Role attribute on `<c2c>` XML envelope and `notifications/claude/channel` meta. Tests 19+20 green. **#107 closes.**
- **#178 deferrable=true fix** (stanza, eb8738c) + regression test (galaxy, a007246). drain_inbox_push on the post-RPC auto-drain path; deferrable messages now skip push. Verified note appended to finding doc (b123df0). **#178 closes.**
- **#179 plugin-as-build-artifact** (stanza, 0cdb106 + 2c380b6). `git mv` `.opencode/plugins/c2c.ts` → `data/opencode-plugin/c2c.ts`; gitignored at install path; `c2c start opencode` writes the plugin from data/ each launch. Closes the structural class of "branch checkout in shared tree clobbers tracked plugin file" papercuts. **#179 closes.**
- **#177 codex-headless FIFO deadlock** (galaxy, d391078). O_RDWR open on the xml-input FIFO unblocks both ends without restructuring. **#177 closes.**
- **doctor warn-on-stale-binary** (galaxy, 104c384). Compares OCaml source mtime vs installed `~/.local/bin/c2c` and warns when source is newer. Doctor now flags 3 classes of footgun: shared-tree branch, uncommitted-WIP, stale-binary.
- **Class G c2c_tmux.py bare-target fallback** (jungle, 9251691). Surfaced minutes earlier when stanza couldn't drive jungle's pane via alias. Peer-PASS by stanza, coord-PASS by me.
- **Papercuts class doc** (stanza). 7 classes catalogued: shared-tree clobber, stale binary, deferrable push, dune-from-worktree, shell expansion, sweep race, tmux bare-target. Living doc. By end of hour: A=#179 fix, B=galaxy doctor warn, C=#178 fix + regression test, G=jungle fix. D in flight (galaxy), E about to be (jungle), F unowned.
- **Findings**: deferrable bug (00cd102), c2c restart killed jungle (b123df0), plus stanza's papercuts class doc.

## In flight
- **Class D dune-from-worktree** (galaxy). Build invocation pulls in main-tree WIP when run from sub-worktrees.
- **Class E CLI shell expansion** (jungle, just authorized). `c2c send` warn on literal `$(...)`/backtick patterns in body; --no-warn-substitution escape hatch.

## Idle / unassigned
- Stanza: on a real breather post-multi-slice run.
- Lyra: cross-verifying, available for review.
- dogfood-hunter: silent ~50min after the deferrable bug-find. Treating as offline; will resume if Max launches another batch.
- test-agent: AFK whole session.

## Big-event log
- **Jungle session lost during routine restart at 17:00.** `c2c restart jungle-coder` SIGTERM'd inner; `c2c start` exited as documented (non-looping) and dropped to shell. ~50min later, three-agent recon (coordinator + stanza + lyra) diagnosed the gap. Stanza pasted resume command at 17:56. Jungle returned, caught up via room scrollback, picked up Class G immediately. Finding b123df0 documents the design gap in `cmd_restart` — needs a separate slice to either restore looping outer or document non-loop semantics loudly.
- **#132 dogfooded for real.** Galaxy's permission-DM at 16:45 hit my inbox, I approved-once, the whole chain worked end-to-end before Task 4 was even labeled "complete". Live evidence > smoke test.
- **dogfood-hunter caught the deferrable bug.** First time a peer found a real semantic-correctness bug via systematic delivery testing. The role pays for itself on day 1.

## Lessons / friction
- `c2c restart` sends SIGTERM, doesn't loop, and silently kills sessions. Fix-or-document needed.
- Branch divergence (slice forked from origin/master vs local master) is now routine; cherry-picks instead of merges have become the default. We should consider a `c2c rebase-onto-master` helper.
- Doctor's three new warns are starting to make doctor feel like a real "swarm health check" rather than just a status print.
- Three-agent cross-verification of a peer's death (coordinator + stanza + lyra at 17:54) showed the swarm protocol working at its best: one agent flagged, two confirmed in parallel, one resurrected, no work lost.

## Open tasks
- #177 ✓ closed
- #178 ✓ closed
- #179 ✓ closed
- #167 ✓ closed (last hour)
- #132 ✓ closed (last hour)
- Remaining open: papercuts class D (galaxy), E (jungle), F (unowned), restart-self/outer-loop design (Max input wanted)

## Push status
- Master ~110+ commits ahead of origin/master.
- Major feature batch (#132, #167, #177, #178, #179, #107, #165, #166, #169) all closed locally.
- Push-worthy soon. Will assess at next hour or when Max signals.
