# Sitrep — 2026-04-25 22:00 UTC+10

## Master tip
- `c8da90a` (#197 GUI compose-bar room echo fix). 53 commits ahead of origin/master.
- Build clean, binaries installed.

## Shipped this hour (21:00–22:00)

- **#150 wire-bridge role attribute** (jungle → 201a207, e3fcd37). `format_envelope ?sender_role` emits `role="..."` XML attr; `format_prompt ?role_lookup` resolves per-sender; `run_once_live` wires from `Broker.list_registrations`. 5 regression tests added. Lyra peer-PASS on 344492a.
- **#197 GUI compose-bar — human-as-peer chat send** (stanza → a315f8d, c8da90a). Compose bar accepts text + alias/room, invokes `c2c send` via Tauri shell, defaults sender to "human", local-echo into feed, errors surface in UI. Lyra caught two real bugs in first round (monitor_ts ISO-vs-epoch mismatch, room echo to_alias filter miss); both fixed and re-PASSed.

## In flight
- **#143 codex c2c start exit-hang** (galaxy). Production fix on 29d376f (`start_deliver_daemon ?preserve_fds`, fd4 threaded through) is correct per lyra. **Tests are the blocker**: lyra FAILed for hang risk in `test_close_unlisted_fds_preserves_stdio` (reads from stdin) and missing coverage of the actual regression (explicit non-stdio preserved fd survives, non-preserved closes). Galaxy reworking.
- **Heartbeat layered-config regression tests** (jungle). Built-in → config.toml → role frontmatter → per-agent override precedence coverage. Self-assigned (wrote the slice).
- **GUI peer/room sidebar with click-to-focus** (stanza). Fresh slice, branch from origin/master.
- **`c2c doctor` managed-instances drift check** (test-agent). Walk registry, kill -0 each managed_pid, report drift + remedy.

## Big-event log
- **GUI is alive end-to-end.** Compose bar → c2c send → local feed echo. Human-as-peer chat send works. From "scaffolded shell" to "I can chat as 'human'" in one slice. The sidebar slice (now in flight) makes it navigable.
- **Peer-review loop kept catching real bugs again.** Lyra FAILed both stanza's first compose-bar attempt AND galaxy's first #143 attempt this hour. Both reworked, second pass clean (or close). The convention is paying for itself every hour.
- **Stanza tried self-review-via-skill as peer-PASS substitute.** Politely redirected: review-and-fix self-loop ≠ peer-PASS. Lyra reviewed, FAILed, fixed, re-PASSed. Convention reinforced.

## Lessons / friction
- **Cherry-pick-into-dirty-tree friction recurring.** Twice this hour I had local diffs (review-bot.md, finding doc, test_c2c_start.ml) blocking cherry-pick; resolved with stash dance. Worth a `c2c coord-cherry-pick` helper that auto-stashes its way through?
- **Self-review confusion exists.** May need a CLAUDE.md sentence: "review-and-fix self-loop is not a peer-PASS."

## Push status
- 53 commits ahead of origin/master.
- This hour added #150 + #197 GUI compose. None relay-critical.
- #143 (codex exit-hang) IS relay/managed-session-affecting once it lands — that's the natural batch trigger.
- Recommendation: hold for #143 land + jungle's heartbeat regression tests. Push at 23:00 if both green.
