# Sitrep — 2026-04-25 16:00 UTC+10

## Master tip
- `0f00901` (#107 Slice 1 fix) → builds + installed.
- Range since 15:00 sitrep: db5b7a5 → 579daa1 → 91d0cdd → 0f00901 (3 commits).

## Shipped this hour
- **#165 main-guard** (`579daa1`, stanza). `c2c start --worktree` now refuses both `master` and `main`. Trivial 2-line fix; self-PASS accepted (called out for future load-bearing work).
- **#107 Slice 1** (`91d0cdd` + `0f00901`, jungle; peer-PASS by stanza). Adds `role : string option` to registration type + `register` MCP tool param + JSON serialization round-trip. Fix-commit caught a missing `registration_to_json` field — good peer-PASS catch.

## In flight
- **#132 Codex permission forwarding** (galaxy, paired with lyra)
  - Task 1 done: `parse_managed_server_request_event` + 7 tests (`b10beec`).
  - Task 2 done: `forward_permission_to_supervisors` + 14 tests (`8bb0bb0` → rebased `d9fc2df`).
  - Live probe blocked by nested-session restriction; authorized to proceed on source analysis (lyra read of `sideband.rs:375-385` + `main.rs:483-505`).
  - Branch `slice/132-codex-permission-forwarding` rebased onto current master.
  - Awaiting lyra re-review on Task 2; then Task 3 (FD 6 named FIFO into `codex-headless`), Task 4 (smoke).
- **#107 Slice 2** (jungle, next): envelope emitter — add `role=` to `<c2c>` XML tag when set, omit when None.

## Idle / unassigned
- Stanza: idle post-peer-review. Acked the worktree-discipline note (had committed in main tree, now committed to using her worktree).
- Test-agent: no response on smoke-probe offer for the `c2c restart` fix (15:29) — may be AFK. Resting on-call.
- Lyra: paired with galaxy; also available as peer-reviewer.
- Jungle: rebasing slice/jungle-next onto master before Slice 2.

## Lessons / friction
- **Branch divergence from forking pre-master.** Both jungle and galaxy started branches from `5495115` before today's master commits landed. Cherry-pick worked for jungle's #107 Slice 1, but creates rebase tax for ongoing work. Surfaced in DMs; both rebased.
- **Worktree topology drift:** main tree (`/home/xertrov/src/c2c`) currently on `slice/132-codex-permission-forwarding` (galaxy/lyra working there). Stanza also briefly committed in main tree before runbook-discipline kicked in. The runbook works only when agents launch via `c2c start --worktree --branch <slice>`; the runbook itself will lower friction over time.
- **Self-PASS vs peer-PASS:** stanza's main-guard self-PASS accepted as trivial precedent. Reaffirmed: load-bearing work routes through a different peer.

## Push status
- Master is many commits ahead of `origin/master`. No urgent need to push (no relay-critical changes since last deploy gate).
- Will accumulate until either (a) #132 Task 3+4 land (relay-touching: changes deliver-daemon FIFO consumption — may want push for swarm-wide pickup), or (b) Max calls for deploy.
