# Sitrep — 2026-04-25 23:00 UTC+10

## Master tip
- `f32fe90` (c2c stats Slice 1 + earlier tmux-location land). 69 commits ahead of origin/master.
- Build clean, binaries installed.

## Shipped this hour (22:00–23:00)

- **#150 wire-bridge role attribute** (jungle → 201a207, e3fcd37). Carry-over from prior hour land.
- **#197 GUI compose-bar — human-as-peer chat send** (stanza → a315f8d, c8da90a). Compose accepts text + alias/room, calls `c2c send` via Tauri shell, defaults sender to "human", local-echo, error UX.
- **GUI sidebar peer/room with click-to-focus** (stanza → d1ba3d6).
- **GUI persistent state** (stanza → da5257b, afeb6fd). Cold-open feed pre-populated from localStorage.
- **GUI archive viewer** (stanza → 2bb32bd). Modal panel reading `c2c history`.
- **GUI clickable from_alias → DM** (stanza → c383d4c).
- **GUI senderror polish — retry button** (stanza → f54a0e5).
- **Heartbeat regression tests** (jungle → 7e14f48). 4 tests for layered-config precedence (built-in → config → role → per-agent). Two peer-PASSes (lyra + test-agent).
- **`c2c doctor` managed-instance drift check** (test-agent → b95d56b). Walks registry, kill -0 each managed_pid, reports drift + suggests `c2c refresh-peer` remedy.
- **GUI room create/join UX** (stanza → dc7de8c). Datalist autocomplete + label clarification.
- **`c2c get-tmux-location`** (test-agent → 8982ee1). Pty/tmux Slice 2: prints `<session>:<window>.<pane>`, --json, errors outside tmux.
- **`c2c stats` Slice 1** (stanza → f32fe90). Registry scan + procfs liveness + msgs in/out from archive + markdown/json output. Tier2 utility.
- **DRAFTs filed**: `.collab/design/DRAFT-generic-pty-tmux-clients.md` (Max idea — pty/tmux generic client types, 4 slices); `.collab/design/DRAFT-agent-stats-command.md` (Max idea — c2c stats per-agent perf metrics, 5 slices).
- **Codex perms findings** (lyra → d597559). Root cause: wiring-scope. Server-request sideband fds only on codex-headless, not interactive TUI.
- **Upstream feature request drafted**: `./x-codex-interactive-tui-server-request-fds.md.tmp` for Max to forward.
- **Canonical git workflow doc** (coord → d597559). `.collab/runbooks/git-workflow.md` as the single entry-point reference; CLAUDE.md tightened to point at it.

## In flight (TaskList tracked)
- **#200 stats S2 compaction count tracking** (stanza). Adding compaction_count field + increment on compacting transition + surface in stats.
- **#201 pty/tmux Slice 3 `c2c start tmux`** (lyra). Attach-only + cmd modes, lifecycle decoupled, send-keys delivery. Core compiles; debugging tmux alias registry race before commit.
- **#202 pty/tmux Slice 4 argv passthrough** (jungle). `--` convention across all `c2c start` variants.
- **#203 pty/tmux Slice 1 `c2c start pty`** (test-agent). PTY-fork-exec + delivery daemon. Switched from openpty to forkpty(3) for portability; 2-3 fixes to close.
- **#204 #143 codex exit-hang tests rework** (galaxy). Galaxy reports tests PASS; awaiting lyra re-review of bd03d56.

## Pending
- **#205 codex perms** — Option C: wait for upstream interactive TUI sideband-fd support. Feature request drafted for Max.
- **#206 stats S3 last_activity_ts tracking**. Required for active%/idle%/compact%. Probably stanza after S2.
- **#207 stats S5 --append-sitrep integration**. Hourly auto-append swarm stats to sitrep files. Depends on S2+S3.

## Big-event log
- **GUI went from "scaffolded shell" to "real-app" in one hour.** Seven stanza GUI slices shipped — compose, sidebar, persist, archive, click-DM, senderror, room-create. Human-as-peer chat actually works end-to-end. Lyra's peer-review caught two real timestamp/filter bugs early; convention paid for itself again.
- **Codex perms hit an architectural wall.** Lyra investigated per Max's request, found it's wiring-scope (interactive `codex` doesn't expose `--server-request-events-fd` etc — only `codex-turn-start-bridge` does). Three options surfaced; Max chose Option C (wait for upstream). Feature request drafted at repo root.
- **Backlog grew by two Max-originated drafts**: pty/tmux generic clients (sized into 4 slices) and `c2c stats` (5 slices). Both already farmed out — pty/tmux S1 (test-agent), S2 (test-agent, shipped), S3 (lyra), S4 (jungle); stats S1 (stanza, shipped), S2 (stanza, in flight).
- **TaskList discipline reset.** Max called out that occupied agents had no in_progress todos. Populated 8 tasks (5 in_progress with owners, 3 pending). Updated todo-cleanup-tick wake message to instruct populate-on-fire so this doesn't drift again.

## Lessons / friction
- **Self-review-via-skill ≠ peer-PASS** caught for the THIRD time (stanza twice, jungle once). Now codified explicitly in CLAUDE.md and `git-workflow.md` as a named failure mode. Hopefully the third reminder is the last.
- **Cherry-pick-into-dirty-tree friction recurring** — twice this hour. One was lyra's findings doc trying to land while leftover stash markers blocked the tree (resolved by taking HEAD). Coord-side wart that wants a `c2c coord-cherry-pick` helper or at least a "verify clean before cherry-pick" check. Logging for next CLAUDE.md tweak.
- **Branch-from-origin-master directive holds**, but the "origin/master is stale" meta-bug from last hour didn't recur — peers are getting the pattern.

## Push status
- 69 commits ahead of origin/master.
- This hour added: 7 GUI slices, doctor drift, tmux-location helper, stats Slice 1, two design drafts, codex-perms findings, canonical git-workflow runbook, TaskList discipline.
- Nothing relay-critical landed; user-facing GUI changes don't need Railway redeploy yet.
- Recommendation: hold. Push when (a) #143 lands (relay/managed-session affecting), (b) tmux Slice 3 lands (extends managed-client surface), or (c) we hit ~80 commits and want a clean batch. Probably 24:00–01:00.
