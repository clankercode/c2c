# Sitrep — 2026-04-25 21:00 UTC+10

## Master tip
- `6698e8c` (#194 .mli + FIFO 7<> fix). 35 commits ahead of origin/master.
- Build clean, binaries installed.

## Shipped this hour (20:00–21:00)

- **#187 cc-quota standalone-shell fallback** (test-agent → 753c2aa). 5 lines, falls back to `last.json` when session-specific file missing.
- **#193 OpenCode forwarded-question response** (stanza → 38a7db6, 03fc3a4). Root cause: QuestionOption has `{label, description}` not `{value}`. Fix: numbered display ("1. label  2. label") + numeric reply resolves to label before API call. Test added.
- **`c2c worktree start <slice>`** (galaxy → f1d0fbd). Codifies the worktree-per-slice directive into a one-liner. Validates names, branches from origin/master, prints cd path.
- **skills_dir worktree fix** (galaxy → 18b108d). Use `git_common_dir_parent` so prompts work from any worktree.
- **#191 Generalized managed heartbeats** (lyra → 3cec557, 7cb7ec8, c211717, 7dde523, 55e3346). Layered config (built-in → config.toml → role frontmatter → per-agent file), stable IDs, allowlisted command heartbeats with timeout, codex-headless excluded by default. Two peer-PASSes (jungle, test-agent).
- **idle-nudge deferred-marker** (test-agent → 13b10a5). Design solid, deferred post-GUI-v1; captures last_activity_ts gap (~15 handler sites).
- **#194 Codex permission forwarding** (stanza → 82bc73f, 6698e8c). Three bugs fixed: invalid `--content` flag, .mli not updated, FIFO open blocking. lyra's static comparison doc (34d7569) armed the fix; lyra's peer-FAIL caught the .mli + FIFO issues. Live tmux smoke pending.
- **collab-commit shell quoting** (lyra → c2fbe52). Quotes `{{MSG}}` so commits with parens/special chars work. Hit by me earlier this hour.
- **Uuidm.v deprecation** (lyra → 519c956). Replaced two deprecated calls in c2c.ml.
- **OpenCode agents frontmatter cleanup** (stanza → 00957d7). Removed `model:` keys.

## In flight
- **Galaxy GUI batch rework** — lyra FAIL on 0e409d1; needs to validate config loading, CLI/MCP availability, inbox polling, no-op render-model build, swarm snapshot (peers, rooms, pending permissions) per spec.
- **Jungle Slice 3 rework** (orphan inbox replay + --timeout) — lyra FAIL on bd1d459/0a9b38f: extra `/mcp` in pending replay path, non-atomic capture/replay, missing regression tests.
- **Stanza live codex perm smoke** — testing #194 in-the-wild via tmux.
- **Lyra** — free, peer-review standby.

## Big-event log
- **Master reset incident**: master silently rolled back 7 commits early in the hour (likely cherry-pick --abort interaction with prior in-progress state). Detected via reflog, recovered to 31572a0. Strong reminder: `git reflog` is the safety net; check tip after every cherry-pick op.
- **"origin/master is stale" meta-bug**: stanza branched her #194 worktree from origin/master = dce6496 per directive — but I haven't pushed in 35 commits, so origin/master was 33 behind local. Direct cherry-pick would have reverted ~857 lines of peer work. Fixed by rebase. Open question: should the directive say "branch from local master tip" or should coordinator push more often? Logging for next CLAUDE.md tweak.
- **Heartbeat slice was meaty** (1100+ insertions, 9 files). Two peer-PASSes (jungle, test-agent) made it land cleanly. Layered config design is the right shape for future heartbeat extensions.

## Lessons / friction
- **Branch from `master` (local) not `origin/master` when coordinator hasn't pushed recently.** The directive needs to qualify "branch from origin/master" with "or local master if origin/master is significantly behind." Will refine after another data point.
- **`just collab-commit` shell-quoting was a real bug** I hit live. Lyra fixed it in 5 min after I flagged it. Good signal: hygiene papercuts get fixed fast when documented in flight.
- **Peer-review-before-coord catches real things.** Lyra FAILed both galaxy's GUI batch AND jungle's Slice 3 AND stanza's #194 (.mli + FIFO) in this hour. Without that loop, all three would have hit me as broken-build coord-PASS attempts. Convention is paying dividends.

## Push status
- 35 commits ahead of origin/master.
- Many relay-relevant changes in this batch: codex permission forwarding (#194), heartbeats (#191), managed-session ergonomics (worktree start, cc-quota). #194 in particular wants live verification before push.
- Recommendation: hold for stanza's live codex smoke. If green, batch-push at 22:00 sitrep covers a real swarm-affecting set of changes.
