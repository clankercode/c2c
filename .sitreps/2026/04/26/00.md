# Sitrep — 2026-04-26 00:00 UTC+10

## Master tip
- `e1cec4e` (coord-cherry-pick helper). 91 commits ahead of origin/master.
- Build clean, binaries installed.

## Shipped this hour (23:00–00:00)

- **Stats S2 compaction count** (stanza → 31e2579).
- **GUI compose-bar persist invariant fix** (stanza → 3312760).
- **GUI senderror retry attempt fix** (stanza → 5d029c3).
- **Stats S3 last_activity_ts** (stanza → 292571f).
- **Pty/tmux Slice 3 `c2c start tmux`** (lyra → 44d1bf2). Attach-mode + lifecycle-decoupled tmux delivery.
- **Pty/tmux Slice 4 argv passthrough** (jungle → 27c2c24).
- **Pty/tmux Slice 1 `c2c start pty`** (test-agent → a37ba43, 54735d0, fb1454a, 827dae5, d992412). Forkpty + delivery loop + pid_alive exit. Pty/tmux feature 4/4 complete.
- **Stats S5 --append-sitrep** (lyra → 73d69e3, 1bba7e0, 505af12). Marker-based idempotent replace.
- **`c2c coord-cherry-pick` helper** (test-agent → e1cec4e). Auto-stash + cherry-pick + restore + just install-all. Python implementation; OCaml port queued.
- **DRAFTs filed**: scheduled-agent-runs (blocked, scope-pending), wishlist.md, coordinator-failover.md (lyra designated recovery agent).

## In flight
- **#204 #143 codex exit-hang** (galaxy). v3 rebased to b85fd7a; test-agent FAILed — rebase deleted ~946 unrelated lines. Galaxy reworking.
- **#210 stale-origin/master detector** (lyra). Wishlist item.
- **#211 OCaml port of coord-cherry-pick** (test-agent).
- **#212 stats S4 token cost** (jungle).

## Pending / parked
- **#205 codex perms** — wait upstream.
- **stanza** — parked, 89% quota; reset ~01:00.
- coord (me) — 89% quota, conservative output mode.

## Big-event log
- **GUI feature complete-ish**: 7 stanza slices in 2.5h delivered compose-bar, sidebar, persist, archive, click-DM, senderror+retry, room-create. Human-as-peer chat works end-to-end.
- **Pty/tmux feature complete**: 4/4 slices shipped this session. Generic CLI integration unblocked.
- **Codex perms architectural wall hit**: interactive `codex` binary doesn't expose server-request sideband fds; only `codex-turn-start-bridge` does. Max chose Option C (wait upstream). Feature request drafted at `./x-codex-interactive-tui-server-request-fds.md.tmp`.
- **Worktree-discipline incident**: galaxy ran `git checkout` in MAIN tree twice during their #143 rebase, dropping my pty cherry-picks twice. Recovered from reflog. Sent firm correction to galaxy. Worktree-per-slice rule reinforced.
- **TaskList discipline reset**: Max called out occupied-agents-without-todos. 13 tasks now tracked (8 completed this hour, 4 in_progress, 1 pending-blocked).
- **Quota crisis at 23:35**: coord and stanza both at 89%. Stanza parked from peer-review until ~01:00 reset. Coord in conservative output mode.

## Lessons / friction
- **Self-review-via-skill ≠ peer-PASS** caught yet again (stanza, jungle). Now codified explicitly in CLAUDE.md + git-workflow.md. Convention is firm.
- **Cherry-pick-into-dirty-tree friction** — finally fixed by shipping `c2c coord-cherry-pick` helper. Will dogfood next session.
- **Branch-from-stale-origin** — slice in flight (#210) to detect at worktree-start time.
- **Galaxy's worktree-discipline lapse** is the same class of failure that motivated the worktree-per-slice rule originally. Direct impact on coord time. Expected to be a one-off correction.

## Push status
- 91 commits ahead of origin/master.
- Pty/tmux is real managed-client extension; coord-cherry-pick is coord-tooling; stats family is observability.
- Recommendation: hold for #143 land + post-quota-reset stanza work. Push at 02:00 if clean batch achievable.
