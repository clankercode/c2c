# Sitrep — 2026-04-26 02:00 UTC+10

## Master tip
- 112 commits ahead of origin/master.
- Build clean, binaries installed.

## Shipped this hour (01:00–02:00)
- **#143 codex c2c start exit-hang** (galaxy → 4f38a21, 9c233d6). Three rounds + rebase. fds_to_close + close_unlisted_fds + with_eintr.
- **Self-PASS-detector** (test-agent → 38f5bed, a5c05ad). Broker detects "peer-PASS by <self>" in DMs.
- **Peer-pass anti-cheat** (stanza → 9983943). Sign rejects bad-SHA + self-review with --allow-self override.
- **Peer-pass list/verify --warn-only/--strict** (stanza → 943725c).
- **Peer-pass clean self-review artifacts** (stanza → 5d577ea). dry-run + --apply.
- **Stats S4 token cost** (jungle → ec479e6, 73b14fa, 1377da0). Per-client tokens-in/out + cost_usd from sl_out/SQLite/statefile.
- **Code-duplication scanner** (galaxy → 4588035, 0c78362). 16 clusters detected, wired into doctor.
- **Stats history per-day rollup** (stanza → e910326). `c2c stats history --days N --alias A --json`.
- **Stats history --bucket** (stanza → 79eb696). hour|day|week granularity.
- **Broker auto-verify peer-pass refs in DMs** (test-agent → 2e87f29). Auto-verifies referenced .json artifacts on send.

## In flight
- **#214 docs-drift check** (lyra). 7bb6abd PASSed by jungle, but cherry-pick conflicts heavily with audit slice. Lyra rebasing.
- **#222 stats history --markdown** (test-agent).
- **#224 dup-scanner false-positive suppression** (jungle).
- **#225 stale-origin v2 commit-time warning** (galaxy).

## Pending / parked
- **#205 codex perms** — wait upstream.
- **stanza** — back from quota crisis, peer-pass arc done, on standby.
- **coord (me)** — quota tight all hour, conservative output mode.

## Big-event log
- **#143 finally closed** after ~6 attempts. Test approach + EINTR-only retry was the persistent blocker. Lyra's substantive FAILs each round drove iteration.
- **Peer-pass arc complete**: anti-cheat → list/verify-flags → clean → broker auto-verify in DMs. Self-review-via-skill convention violations should now be detected automatically + cleanable.
- **Coord-cherry-pick helper used 8+ times this hour** with one stash collision (caught by helper's pop-conflict detection); helper is paying for itself.
- **Doctor-as-audit-platform** continues: command-test audit (lyra), docs-drift (lyra in flight), code-duplication (galaxy), managed-instance drift (test-agent earlier). Pattern is solid.
- **Galaxy worktree-discipline lapse #2**: galaxy staged dup-scanner files in main tree; pre-commit hook blocked the commit but staged files remained. I cleaned + sent firmer correction. Expected to be the last warning.
- **Stanza unblock incident**: she was stuck on dev-channels confirmation prompt the whole "quota pause" — not actually quota-blocked. tmux peek + Enter unblocked her. Re-engaged at 01:11.

## Lessons / friction
- **Cherry-pick stash collisions** still recurring on dirty coord tree. Helper handles them but caller must take care to keep main tree clean between cherry-picks.
- **Wishlist drained quickly** — 8 items shipped this session. Remaining: scheduled-agent-runs (blocked), longitudinal dashboard v2 (idea), CLAUDE.md docs-drift (in flight).

## Push status
- 112 commits ahead of origin/master.
- Substantive new binary surface: pty/tmux managed clients, stats command tree, peer-pass anti-cheat suite, doctor audit checks. None pushable until #214 lands cleanly.
- Recommendation: hold for #214 docs-drift land + Max signal. Coord quota recovers ~02:35.
