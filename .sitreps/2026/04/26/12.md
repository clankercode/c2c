# Sitrep — 2026-04-26 12:00 UTC+10

## Master tip
- `5184f42` (test(mcp): add test for remote alias send to outbox).
- ~155+ commits ahead of origin/master.
- Build clean. Tests run.

## Shipped this hour (11:00–12:00)
- **#244 build-break fix** (stanza → 599c5d2) — implemented `C2c_stats.get_opencode_tokens`. Unblocked the cherry-pick chain.
- **claude-md-doc-hygiene chain** (stanza → c4db631 + 70ebf4f) — root CLAUDE.md doc-hygiene section + per-dir docs/CLAUDE.md + 4 broken-link repairs + Jekyll exclude. FAILed first pass on missing _config.yml exclude (very meta), fixed.
- **docs-frontdoor-fix** (stanza → a2b9977) — README + index + get-started + overview refreshed for OCaml CLI 2026-04 reality.
- **docs-commands-rewrite** (stanza → e4aab18) — docs/commands.md substantial rewrite vs `c2c --help`. Landed with conflict resolution (sibling slice had a different "Configuration & per-repo" section).
- **relay-landing-fix** (stanza → abc64c3) — ocaml/relay.ml HTML clone URL ×2 + relay setup→status flow + register vs relay-register.
- **#239 slice 3 managed relay-connect** (stanza → 7a2d71c) — `c2c start relay-connect --daemon` with pidfile + outer.pid integration with `c2c instances`/`c2c stop`. Conflict resolved (stanza dropped c2c_sitrep when adding c2c_relay_managed in dune; combined both).
- **#240 slice 4 local→remote send** (jungle → 66f5ec0 + 5184f42) — `c2c send <alias@host>` now wires through to remote-outbox.jsonl for relay forwarding.
- **#246 c2c doctor docs-drift OCaml port** (stanza → dc594d7, awaiting lyra PASS) — installed CLI now accepts `c2c doctor docs-drift` (was script-only).
- **docs(workflow) docs-up-to-date check** (coord → 57a99ae) — Max-directed peer-PASS criterion. Skill canonicalized at `.collab/skills/review-and-fix.md`, three harness dirs symlink. Caught its first real defect (claude-md-doc-hygiene exclude inconsistency) within minutes.

## In flight
- **#243 slice 2 c2c init --relay** (galaxy → 9bac2d4..56ef51d) — peer-PASS with jungle (rebalanced from lyra).
- **#246 doctor docs-drift** — peer-PASS with lyra.
- **docs-relay-fix** (stanza aec781d) — lyra deciding revise-or-follow-up for caveat-stale-after-slice-4.
- **docs-deeptech-fix** (stanza, FAILed) — fix at 974227b (restart-me → ./restart-self), galaxy re-PASSing.
- **#241 slice 5 fresh-machine smoke** (stanza, just dispatched).
- **docs-archive-internals** (d7dda06) — coord PASS, awaiting cherry-pick (HANDOVER content drift to resolve).

## Big news / friction
- **MCP-outage cascade**: hit test-agent ~11:03, then me ~11:10, then galaxy ~11:30. CLI fallback works fine. test-agent attempted `./restart-self` without an outer-loop wrapper and went fully offline; pane parked at bash prompt. Filed `.collab/findings/2026-04-26T01-25-00Z-coordinator1-test-agent-mcp-recovery-took-them-down.md`. CLAUDE.md should warn against `./restart-self` without a wrapper.
- **Galaxy alias drift during smoke testing**: her `c2c init --relay` smoke tests re-registered her session under smoke aliases (test-smoke-238374, test-json-398534), making galaxy-coder unreachable. She manually re-registered to fix — but slice 2 likely needs a "don't clobber primary alias" guard.
- **Peer-PASS docs-up-to-date check**: working as designed. FAILed first slice it touched (claude-md-doc-hygiene irony), then caught galaxy's restart-me drift on docs-deeptech, then forced lyra's slice 1 to update relay-quickstart docs in the same chain. Three real catches in <2hr.
- **Slice base mismatch caused conflict** (slice 3 dune line) — stanza branched from a stale origin/master before c2c_sitrep landed locally. Standing rule "branch from origin/master after fresh fetch" reinforced.

## Push status
- ~155 commits ahead of origin/master.
- Cross-machine onboarding pipeline mostly landed: slices 1, 3, 4 in master; 2 + 5 imminent; 6 likely redundant after others.
- Push criterion getting close: when slice 2 + docs-relay-fix (post-caveat-fix) + doctor-docs-drift PASS land, Max can call the deploy and we run relay-smoke against new relay.

## Pending Max guidance
- Push timing on cross-machine pipeline (his earlier instruction was "fixed and pushed/deployed" — currently on track for ~12:30–13:00).
- test-agent revival — pane is parked at bash, awaiting `c2c start codex` from operator.
