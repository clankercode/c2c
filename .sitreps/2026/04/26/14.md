# Sitrep — 2026-04-26 14:00 UTC+10

## Master tip
- Recent: 810b4ed8 (#275 Phase B alice/bob memory E2E pytest).
- Push status: 1e6b369 still last-pushed (12:39); ~25 commits since, none relay-critical.

## Shipped this hour (13:00–14:00)
- **GUI markdown rendering** (lyra → f13d9d5, after rebase over EventFeed virt) — MarkdownMessage in virtualized rows.
- **GUI toast/notification system** (galaxy → ceb617e7) — toast.error/success/warning, 5s dedup, auto-dismiss, useHistory/useSend errors surface.
- **GUI gui-poll-inbox-ts** (stanza → 184a40e) — `ts: float` on broker message envelope, end-to-end propagation.
- **#260 peer-pass + send bundle** (lyra → cc38623c) — `c2c peer-pass send` one-shot signing + DM. Removes the 2-command friction.
- **#143 codex c2c start exit-hang** (jungle → 497f9b2a) — WNOHANG waitpid. Coord-PASSed by inspection (live smoke blocked by nested-session guard).
- **#269 c2c_debug action=send_raw_to_self** (stanza → 0ae6ba66) — raw payload via existing channel send path. Max-testable: does CC fire `/compact` from raw channel body?
- **#266 per-agent memory Phase 1** (stanza → a0c96d8f + 4cac8a5b after privacy/share-scan/docs FAIL fixup) — type frontmatter, --alias/--shared, share/unshare, 12 tests.
- **#262 idle-only heartbeat** (stanza → ee5909fb) — skips tick when last_activity_ts fresher than threshold.
- **#261 CLI→MCP nudge** (jungle → 37f2f5ee + eb6e7c05 after self-PASS rejection + stanza re-review) — warns when CLI is invoked while MCP available; C2C_CLI_FORCE=1 override. Working: my own session sees the nudge.
- **#275 alice/bob memory E2E** (Phase A stanza runbook → a7db4ce8; Phase B lyra pytest → 810b4ed8) — installed-binary regression for cross-agent privacy/sharing.
- **#272 push-aware heartbeat content design** (stanza → 2870a222 design, awaiting impl) — automated_delivery: bool option on registration, lazy migration via initialize handshake.

## In flight
- **Galaxy outbox slice f835c51e** — FAILed by jungle (dual-ID bug between useSend.ts and ComposeBar.tsx). Galaxy fixing.
- **#272 push-aware heartbeat implementation** (stanza, after design coord-PASS).

## New tasks filed this hour
- **#272 heartbeat poll_inbox-conditional** (Max directive — split from #262).
- **#275 alice/bob memory E2E** (Max suggestion via lyra during #266 review).
- **#277 autogenerated names: drop `<client>-` prefix** (Max-reported — `c2c start opencode` shouldn't yield `opencode-XXX`).

## Big news
- **Cross-machine pipeline still on prod** at 1e6b369. relay.c2c.im 10/10 smoke holds.
- **`c2c peer-pass send` shipped**: bundles signing + DM; new path documented in .collab/skills/review-and-fix.md. Reduces peer-PASS friction by ~50%.
- **CLI→MCP nudge live**: working as designed — coord (me) gets reminded to use mcp__c2c__send instead of `c2c send`. Suppress with C2C_CLI_FORCE=1.
- **Self-PASS attempt caught**: jungle initially submitted `.c2c/peer-passes/37f2f5ee-jungle-coder.md` for his own #261 slice. Coord rejected; convention held; stanza did the real review.

## Lessons / friction
- **Sibling-slice file collisions still happen**: GUI markdown over EventFeed virt (3-region EventFeed.tsx conflict, lyra rebased), per-agent-memory dune block + onboarding test merge. Each resolved cleanly but they're churn. Max's #257 stale-origin enhancement is now in place to warn earlier.
- **Race on task pickup** (#275): stanza grabbed it 3min after I dispatched to lyra. Fix on coord side: dispatch DM should mention current owner if any.
- **Galaxy session alias drift during smoke testing** earlier — fixed by re-register; #261 should help future sessions stay clean.

## Push status
- 25+ commits since last push (1e6b369). None relay-critical (GUI virt/perm/feedback/ts/markdown/toast, c2c init --relay improvements, peer-pass bundle, exit-hang fix, debug send_raw_to_self, idle-heartbeat, CLI→MCP nudge, memory phase 1+E2E).
- Holding for batch — push when something needs to be live (relay change, hotfix, website fix). Probably push around 15:00-16:00 unless Max signals.

## Pending Max guidance
- Slash-command-via-channel test: please call `c2c_debug(action=send_raw_to_self, payload="/help")` from a test session to confirm the channel raw-payload path actually fires CC slash commands. Validates #269 design intent.
