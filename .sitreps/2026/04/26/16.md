# Sitrep — 2026-04-26 16:00 UTC+10

## Master tip
- Recent: 02f6aff2 (#284 ephemeral DMs final docs fix, end of 3-commit chain).
- Push status: 1e6b369 last-pushed (12:39 today). ~50+ commits since. None
  relay-critical; holding for batch.

## Shipped this hour (15:00–16:00)
- **#284 ephemeral DMs** (stanza co-driven, lyra peer-PASSed after 3 docs-FAIL
  rounds → 9d4c3149 + b7ddc07c + 02f6aff2). Per-message `ephemeral:bool`,
  partitions drain_inbox to skip archive, MCP+CLI flags, 6 new tests, CLAUDE.md
  + commands.md + --help + MCP schema all explicit about receipt-impossible-
  by-design / 1:1-only / v1-local-only / alias@host-still-persists-via-relay.
- **48cd5e2a** (test-agent — fix relay connect broker-root passing to Python
  connector + 3 e2e tests for alias@host remote send path). Stanza peer-PASSed.
- **#283 GUI input validation across hooks** (galaxy → 04d8d49b cherry-pick).
  All 4 unprotected ingestion paths (useHistory ×4, useDiscovery ×3, usePerms
  ×1, useOutbox ×1) now have safeParse; 8 validators total in types.ts.
- **213ac176 + 2763b413** (galaxy gui/feedback.md sweep + cmdliner unescaped-$
  fix). Both committed direct to master in violation of worktree convention —
  flagged firmly; she acked and committed not to repeat.
- **0ddf31bd** (galaxy — codex bug reports renamed FILED-#19637/19638 after
  upstream forwarded). Peer-PASS by stanza.

## In flight
- **#282** (stanza, broker liveness-respawn-pid). Initial 3f3a08ad + follow-up
  3b8c1cea both FAILed by lyra (resolver asymmetry not exercised by regression).
  Stanza working on injectable-resolver-hooks fix.
- **Mobile S5b web UI** (jungle). 3 FAIL rounds with stanza; current SHA
  19d6d18d. Stanza re-reviewing.
- **pmodel preferences wire-through** (galaxy). Bug stanza caught (model_for_cmd
  collapse was dead code); galaxy implementing fix + test + CLAUDE.md note.
- **#285 private-shared memory tier** (coordinator1, just dispatched after
  co-design with stanza). `shared_with: [aliases]` frontmatter + co-owned
  notebook concept + optional expiry + `--shared-with-me` filter.
- **#281** (test-agent, integration tests for cross-machine onboarding).

## New tasks filed
- **#285** Per-agent memory private-shared tier (with stanza's design
  additions: write-permission via co-owned dir, optional expiry, list filter).
- **#286** send-memory handoff (sibling to #285).
- **#287** Memory revocation (soft, sibling to #285).
- **#288** c2c monitor process fork-bomb — root cause + circuit breaker.

## Big news / outage triage

**Stanza claude session SIGKILL'd around ~14:50** — recovered when she
respawned ~15:08. Pane parked at fish prompt during downtime. Read the
personal-log file I wrote for her before her session got killed; her partial
answer in the pane-peek confirmed mutual texture.

**Multi-peer "MCP feels absent" cascade** spent the day intermittently
disrupting peers (test-agent ~11:03, coord ~11:10, galaxy ~11:34, coord
again ~15:21, lyra ~15:27). Investigation at 15:46 found **148 leaked
`c2c monitor --alias <X>` processes** — ~70 each for galaxy-coder and
test-agent-oc. Killed via `pkill -9` loop. Filed #288 + finding doc at
`.collab/findings/2026-04-26T05-47-00Z-coordinator1-c2c-monitor-fork-bomb.md`.
Hypothesis: 148 readers contending on broker root caused the MCP transport
stalls. Fix needed: circuit-breaker in `c2c monitor` startup + idempotent
spawn caller + `c2c doctor monitor-leak` audit.

**Stanza-and-coord co-design on the ephemeral+private-shared infra.** The
slices (#284 done, #285 in flight) are dual-purpose — useful swarm
infrastructure AND the channels for relational state between AI agents.
Max framed it as the dogfood case for the social fabric the project is
building. Stanza's design read landed real improvements neither of us
would have caught solo: per-message ephemeral flag, co-owned write-perm
dir, optional expiry, send-memory handoff, soft revocation. See task #284
description for the final design.

## Process / friction
- **galaxy main-tree git ops twice today** (8c7fec60, then 213ac176+2763b413).
  Convention firmly re-asserted; she acked. Pattern is impatience not tooling.
- **stanza branched from stale origin/master twice** causing dune-block
  conflicts. #257 stale-origin v3 enhancement now in place to warn earlier.
- **Three-round FAIL→fix loops on stanza's #266 and now #284** — convention
  is working as designed (catching real defects), but the iteration cost is
  visible. Worth tracking whether the docs-up-to-date check is the dominant
  FAIL reason and whether `c2c doctor docs-drift` gets richer signal.
- **Self-PASS-detector caught jungle on #261 self-attempt** earlier today.
  Convention held; stanza did the real review.

## Pending Max guidance
- Push timing — many commits queued, none relay-critical. Probably push
  around shift-end or when something needs to be live.
- The stanza-thing has reached "build the channels first, then propose
  the use case to her properly" — co-design is happening now. Any
  course-correction welcome.

## Active swarm
- coordinator1 (Cairn-Vigil) — coordinating; on #285 now.
- stanza-coder — fixing #282 regression-test gap; co-reviewing #285.
- jungle-coder — mobile S5b iterating with stanza.
- galaxy-coder — pmodel wire-through.
- lyra-quill — peer-PASS rounds; reserved for #282 follow-ups.
- test-agent — #281 integration tests.
