# Sitrep — 2026-04-26 13:00 UTC+10

## Master tip
- Master at gui-poll-inbox-ts cherry-pick (`ts` field on broker message envelope).
- **Pushed to origin/master at 12:39** (1e6b369). Railway rebuild kicked off; relay-smoke-test 10/10 PASSED against deployed relay.

## Shipped this hour (12:00–13:00)
- **All 6 cross-machine slices landed + pushed**. Slices 1-5 + redundant 6. Live on relay.c2c.im.
- **#246 c2c doctor docs-drift OCaml port** (stanza → 64ef1aa) — installed CLI now accepts `c2c doctor docs-drift`. Lyra + galaxy both PASSed.
- **#234 failover drill consolidated** (test-agent → c3ef9ec) — folds in MCP-outage + SIGUSR1 recovery + parked-shell-after-restart-self failure mode.
- **#257 stale-origin enhancement** (stanza → 8199f66 + 1e6b369 fix) — warns when local master is behind origin/master. Conflict-resolution lossy via -X theirs lost some functions; restored in fix-up commit.
- **GUI permission panel** (galaxy → c74ade4) — TTL countdown, inline approve/deny, history tab, dedup.
- **GUI EventFeed virtualization** (galaxy → 8056920) — useVirtualizer for global + focused views, scroll guard.
- **GUI feedback closeout** (stanza → e82956f) — both GUI bugs from feedback.md were already fixed; doc cleanup.
- **GUI gui-poll-inbox-ts** (stanza → 184a40e) — `ts : float` on broker message envelope, end-to-end propagation.
- **Slice 4 docs follow-up** (jungle → eab51e6) — alias@host remote-routing docs in c2c send help/MCP schema/relay-quickstart.

## In flight
- **#258 codex c2c start exit-hang** (jungle).
- **#264 onboarding integration tests** (test-agent → e7e882a, peer-PASS with jungle).
- **#265 GUI markdown rendering** (lyra) — rebase requested due to EventFeed.tsx 3-region conflict with virt slice.

## Pending / parked
- **#266 DRAFT-per-agent-memory Phase 1** — parked till stanza returns (quota pause).
- **#258 #143 codex exit-hang** — jungle in-flight.
- **#260 peer-pass + send bundle** (Max directive — reduce 2-command friction).
- **#261 CLI nudges to MCP when MCP available** (Max directive — env var override).
- **#262 idle-only heartbeat** (Max directive — track via statusfile).
- **#263 `c2c start --agent X -n Y` agentfile-mismatch bug** (Max-reported).

## Big news
- **Pipeline shipped**: cross-machine onboarding shipped to prod end-to-end. relay-smoke 10/10 against deployed relay validates it. This is the major north-star milestone for today.
- **Peer-PASS docs-up-to-date check working as designed**: caught 4 real defects this session (claude-md-doc-hygiene exclude inconsistency; galaxy's restart-me drift; jungle's send help missing alias@host; lyra's docs-relay caveat stale-after-slice-4).
- **MCP-outage cascade resolved**: SIGUSR1 to OpenCode process is the recovery signal. test-agent documented; both runbook and CLAUDE.md should mention.
- **Stanza paused for quota**, lyra on rebase, jungle juggling #143 + peer-PASSes, galaxy juggling GUI work.

## Push status
- Pushed 1e6b369 at 12:39. Railway rebuild successful (per relay-smoke-test 10/10).
- Hour-since-push commits: ~10 small slices since (GUI virt, perm panel, GUI feedback, gui-poll-inbox-ts, etc). Not relay-critical, hold for next batch.

## Lessons / friction
- **Cherry-pick conflicts proliferate when multiple sibling slices touch the same file** (EventFeed.tsx for virt + markdown; ocaml/cli/dune for stats/relay-managed/docs-drift modules). Solution: more frequent push so slice bases are fresh.
- **`-X theirs` cherry-pick strategy is lossy** (lost worktree_behind_origin + check_all_worktree_bases functions; required fix-up commit). Avoid; prefer manual conflict resolution.
- **Reviewer-attribution ambiguity**: galaxy initially submitted "PASS by galaxy" on her own slice (later clarified as jungle's review). The new self-PASS-detector saved the day on broker side; convention should be tightened.
