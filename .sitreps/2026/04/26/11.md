# Sitrep — 2026-04-26 11:00 UTC+10

## Master tip
- `57a99ae` (docs(workflow): add docs-up-to-date check). 8 cherry-picks queued behind it (4 awaiting peer-PASS, 4 awaiting earlier in chain).
- Build status: master is broken (`test_c2c_stats.ml` references `C2c_stats.get_opencode_tokens` which jungle's S4 #212 added the test for but never landed the function). Stanza fixing in `slice/fix-opencode-tokens-build` (5497824) — gates the cherry-pick chain.

## Shipped this hour (10:00–11:00)
- **#205 codex perms wire-through** (lyra → 33863ca) — interactive TUI `--xml-input-fd` + sideband fds wired post-Codex 0.125.0 stable.
- **#237 cross-machine onboarding gap review** (lyra → 9f00b8d) — 361-line findings doc identifying 9 gaps + draft runbook + 6 implementation slices.
- **docs(workflow) docs-up-to-date peer-PASS check** (coord → 57a99ae) — Max-directed; reviewers FAIL slices that change documented surfaces without updating docs in the same slice. Skill canonicalized at `.collab/skills/review-and-fix.md`, three harness dirs symlink to it (single source of truth across .claude/.codex/.opencode).

## In flight
- **#238 slice 1 cross-machine** (lyra) — relay config load fix. Branch tip f3ed256 (chain d7cbff1+f3ed256 after worktree reset + docs update). Jungle re-PASSing.
- **#240 slice 4 cross-machine** (jungle) — local send vs relay DM investigation.
- **#243 slice 2 cross-machine** (galaxy, staged) — `c2c init --relay URL`. Will dispatch when slice 1 lands.
- **#244 build-break fix** (stanza → 5497824) — implements `get_opencode_tokens`. Awaiting jungle peer-PASS. Gates everything.
- **stanza claude-md-doc-hygiene chain** (0f746ff..c4311bd) — coord PASS landed (FAILed first pass on missing `_config.yml` exclude; fix in c4311bd).
- **docs-deeptech-fix peer-PASS** (galaxy) — reviewing stanza's 4f0bdb3 inline.
- **#234 test-agent failover drill** — testing CLI-fallback when MCP is down.

## Pending
- **Stanza 7-slice docs-sweep batch** from Max's c2c.im audit (#245). Distribution: relay-landing-fix + docs-relay-fix → lyra; docs-frontdoor-fix + docs-commands-rewrite → jungle; docs-deeptech-fix → galaxy (in flight); docs-archive-internals → coord.
- **#246 c2c doctor docs-drift OCaml port** — installed CLI doesn't accept it; lyra had to run a script directly.
- **#239/#241/#242 cross-machine slices** — managed relay-connect, fresh-machine smoke, docs cleanup. Blocked on slice 1+2+3.

## Big news
- **Cross-machine onboarding now has a clear runbook + 6-slice fix plan.** Lyra's findings doc identified that `c2c relay setup` writes `relay.json` but `resolve_relay_url`/`_token` only honor flags + env. Slice 1 (her own) fixes the foundational config plumbing; queue walks naturally from there to one-shot `c2c init --relay URL` + managed connector + smoke test + docs.
- **Peer-PASS process upgrade**: docs-up-to-date check now mandatory. Caught its first real defect immediately — stanza's `docs/CLAUDE.md` claimed to be excluded but `_config.yml` had no exclude entry. FAILed, fix in new commit, re-PASSed.

## Push status
- ~145+ commits ahead of origin/master (post-merge -s ours reconciliation last hour).
- This hour: ~10 commits queued for landing once build-break unblocks.
- Recommendation: hold push until cross-machine slices 1-3 + docs sweep land. Max called out target = "all six slices fixed and pushed/deployed". Probably +30-60 min after build-break clears.

## Lessons / friction
- **`c2c doctor docs-drift` not in installed CLI** — gap surfaced by the very rule we just made everyone use. Filed #246.
- **Build break in master** went undetected because nobody runs `dune runtest` between cherry-picks; install-all only does `dune build`. Worth wiring tests into `just install-all` or a pre-merge check.
- **Conflict between sibling docs slices** (claude-md-doc-hygiene + docs-archive-internals both add `exclude:` to `_config.yml`) — manageable at cherry-pick time but a clean re-base scheme would be nicer.
