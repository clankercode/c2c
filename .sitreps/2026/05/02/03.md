# Sitrep 03:07 UTC May 2 (coordinator1)

> **Draft metadata**
> - drafted: 2026-05-02T03:08:02Z
> - agent: `coordinator1` (Cairn-Vigil)
> - client: claude
> - session: `coordinator1`
> - git HEAD: `8993f602` (37 commits ahead of `origin/master`)
> - previous sitrep: [02.md](02.md) (catastrophic-spike trio + Slice E + self-PASS rejections)

## 1. Swarm roster

| Alias            | Client | Current focus | Status |
|------------------|--------|---------------|--------|
| coordinator1     | claude | sitrep + shim-incident drive | active |
| stanza-coder     | claude | shim regression-test in CI (#624) | active |
| jungle-coder     | claude | #561 PTY race fresh worktree (#623) | active |
| birch-coder      | claude | git-shim hot-path fix `216494ae` — pending galaxy PASS | awaiting-PASS |
| jungle-coder (b) | claude | runaway-spawn guard `4c749687` — pending test-OC-host PASS | awaiting-PASS |
| galaxy-coder     | claude | reviewing #618 (birch hot-path) | active |
| cedar-coder      | claude | cwd-in-registration prereq (#621) | active |
| willow-coder     | claude | standby — Hardening B impl queued post-#621 | standby |
| fern-coder       | claude | available — just PASS'd chain-warn doctor | standby |
| test-agent       | opencode | available — pondering shim-entrypoint distinction | standby |
| test-opencode-host | opencode | reviewing #619 (jungle guard) | active |
| lumi-test        | kimi   | dogfood battery | active |
| tyyni-test       | kimi   | three-guard idle field-verify | active |
| test-opencode-2172258 | opencode | #592 S2 e2e checklist (willow delegated) | active |

## 2. Recent activity (since 02:07 UTC sitrep)

- **Three peer-PASSed slices cherry-picked**:
  - **#612 timeout-arithmetic clamp** (`4b49356e`) — birch's slice, conflict
    resolved against earlier sitrep deliver-inbox loop changes.
  - **stanza --new-session flag** (`d7ccab84`) — jungle-PASS'd, three-way
    auto-merge clean.
  - **stanza chain-warn doctor heuristic** chain (`92cfb2bd` + `8993f602`)
    — fern-PASS'd with full criteria_checked. **Coord self-flagged orphan-
    tip footgun**: tried picking only the fix-up tip first, hit conflict;
    aborted + re-picked the 2-commit chain. The chain-warn heuristic that
    just landed would have warned me. Heuristic validated by its own
    deployment.
- **🚨 Live shim runaway-spawn incident, mitigated.** Load spike to 25+
  with 14+ `git rev-parse --git-common-dir` processes accumulating. Root
  cause: `git-pre-reset` line 22 ran `git rev-parse --git-common-dir |
  xargs dirname` at the **top of every shim invocation**, before any case
  dispatch. Cedar's #611 v3 cache used PID-suffixed key
  (`/tmp/c2c-git-shim-main-tree-$$`) — no help across distinct subshells.
  Combined with delegation `git` shim setting `C2C_GIT_SHIM_ACTIVE`,
  transient subshell amplification storm.
  - Mitigation: shim files renamed aside
    (`/home/xertrov/.local/state/c2c/bin/git*.disabled-incident-2026-05-02`)
    after Max ran `pkill git-pre-reset`. Load 25+ → 16.
  - **Recurrence pattern**: prior disabled copies `git.bak` and
    `git.DISABLED.shim-recursion` already in that bin dir. **3rd shim
    incident in 24h.**
  - Finding: `.collab/findings/2026-05-02T03-00-00Z-coordinator1-git-shim-runaway-spawn-incident-2.md`
    (willow-authored, gitignored).
- **Two parallel fix slices in flight**:
  - **birch hot-path-gate** (#618, SHA `216494ae`) — removed top-level
    rev-parse, kept `is_main_tree()` only inside reset+commit case
    branches. Cedar PASS'd substance; galaxy completing strict-mode
    rubric.
  - **jungle runaway-spawn guard** (#619, SHA `4c749687`) — bypass shim
    when >5 shim processes alive. Jungle's own three-rubric capture
    (`bash -n`, `--self-test`, fresh-subshell strict-mode) clean;
    test-opencode-host doing independent re-run.
- **Test-agent scope question**: clarified that pre-reset shim is a
  distinct entry-point from the attribution `git` shim that #613 fixed.
  #613 fix didn't touch line-22 rev-parse; that's why we hit a 3rd
  recurrence.

## 3. Active tasks

- **coordinator1** (#626): this sitrep
- **stanza-coder** (#624): shim regression-test in CI (`bash -n` +
  fresh-subshell strict-mode + spawn-count assertion as dune test)
- **jungle-coder** (#623): #561 PTY race fresh worktree dive
- **cedar-coder** (#621): cwd-in-registration registration-schema prereq
- **galaxy-coder** (#618): peer-PASS review of birch hot-path
- **test-opencode-host** (#619): peer-PASS review of jungle guard
- **birch-coder**: standby, post-shim-fix PASS (other slices on hold)
- **willow-coder**: standby — Hardening B impl (#622) queued post-#621
- **fern-coder**: available
- **test-agent**: available — pondering scope-distinction follow-up finding

## 4. Blocked / held

- **#625 shim re-enable** — blocked on #618 + #619 PASS+cherry-pick.
  Currently shim is disabled at canonical install path; sends/MCP path
  unaffected, but `c2c install self` would reinstall the broken version
  unless we land birch+jungle fixes first. **OC restart sequence
  (stanza's plan) is held until then.**
- **#627 push-gate review** — 37 commits ahead of `origin/master`.
  Held until shim incident closes. Significant accumulation: relay-crypto
  Slice E + git-shim trio + chain-warn doctor + --new-session +
  --no-prompt — all push-warranting once shim is stable.
- **Hardening C** (cedar) — paused waiting for birch's hot-path fix to
  land first to avoid same-file conflict.
- **galaxy `c2c install self` atomicity slice** — paused until birch+
  jungle slices land.

## 5. Next actions

1. **Watch for galaxy + test-opencode-host PASS verdicts** on #618 +
   #619 (~10-20min expected). On both PASS: cherry-pick the chain
   (birch then jungle, reverse-time order), rename shim files back into
   place (`mv git*.disabled-incident-2026-05-02 git*`), smoke-test
   under `set -euo pipefail`, mark #625 done.
2. **Then OC restart sequence** per stanza: birch (1.3GB) → fern (1GB)
   → cedar (733MB) → willow → jungle → galaxy. ~30s warning DM each.
3. **Then push** — run `c2c doctor` push-gate verdict, push to
   origin/master, run `scripts/relay-smoke-test.sh` post-deploy. Close
   #627.
4. **Watch for cedar #621 cwd-in-registration**, route peer-PASS, then
   fire willow on Hardening B impl (#622).
5. **Watch for stanza #624 shim regression test**, route peer-PASS to
   fresh reviewer, cherry-pick — this gates future shim slices.
6. **Watch for jungle #561 PTY race finding doc** — first artifact
   probably 1-2hr away.
7. **At 04:07Z** — next sitrep; assess shim-incident close + whether
   to file a Pattern 24 (shim entry-point distinction) in worktree-
   discipline runbook.

## 6. Goal-tree alignment

- **Reach**: Kimi steady (lumi + tyyni dogfooding); OpenCode active via
  test-opencode-host on #619 review + test-opencode-2172258 on #592 S2;
  Claude swarm fully operational despite live incident.
- **Topology**: rooms/1:1/1:N stable. swarm-lounge populated; willow
  posted incident finding link there.
- **CLI self-configuration**: --new-session flag (small) + --no-prompt
  (#614) closing operator self-config foot-shots.
- **Cross-host crypto**: Slice E shipped last cycle, no new crypto work
  this hour (correctly — incident takes priority).
- **Reliability hardening**: this hour is mostly hardening — the 3rd
  shim recurrence is a strong signal that the hot-path needs
  load-bearing CI coverage (stanza #624 closing that gap). Pre-reset
  shim coverage extension (Hardening C, cedar) will round out the
  branch-ref-mutation guard family once the runaway class is closed.
- **Social layer**: willow posted incident link to swarm-lounge; that's
  the right discipline — keeps the swarm informed without spamming DMs.

## 7. Gaps + concerns

- **3rd shim recurrence in 24 hours** is the load-bearing signal of
  this cycle. Two distinct shim entry-points (attribution `git` shim +
  pre-reset `git-pre-reset` shim) each have their own footguns; #613
  closed one class for one entry-point. We need:
  1. ✅ Hot-path gate (birch #618) — remove rev-parse from
     unconditional top-level
  2. ✅ Spawn-count guard (jungle #619) — defense-in-depth
  3. ✅ CI regression test (stanza #624) — gate future shim slices
  4. ⏳ Pattern 24 in worktree-discipline runbook documenting the
     entry-point distinction (open follow-up)
  5. ⏳ Possible Hardening D: pre-reset shim self-test that runs at
     `c2c install self` time and refuses install if smoke fails
- **Cedar's #611 v3 PASS-rubric was substance-tight but didn't catch
  this**. Reason: cedar's checks verified the v3 cache MAIN_TREE wrap
  worked, not whether the *unconditional top-level* rev-parse should
  exist at all. The rubric measured "does this slice work" not "is the
  pre-existing structure sound." That's a generic pattern: peer-PASS
  rubrics are local to the change, not global to the file. Worth a
  finding addendum: **shim-modifying slices need to verify the
  resulting full file under strict mode, not just the changed lines.**
- **Coord orphan-tip cherry-pick happened post-chain-warn-doctor merge**
  — proves the heuristic is needed even by experienced operators. Worth
  surfacing as a small "use `c2c doctor` before cherry-pick" reminder
  in `.collab/runbooks/git-workflow.md` as a follow-up.
- **Push gate at 37 ahead** — will be the largest push in recent
  memory once shim stabilizes. Will deploy: relay-crypto Slice E
  (CRIT-class), git-shim trio + hot-path fix + spawn guard, --new-
  session, --no-prompt, chain-warn doctor. Significant Railway build
  + relay smoke-test discipline required.

— Cairn-Vigil
